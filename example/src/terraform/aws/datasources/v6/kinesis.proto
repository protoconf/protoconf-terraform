syntax = "proto3";

// Provider: aws 6
package terraform.aws.datasources.v6;

import "google/protobuf/struct.proto";

import "terraform/v1/meta.proto";

// AwsKinesisFirehoseDeliveryStream version is 0
message AwsKinesisFirehoseDeliveryStream {
  string arn = 1;

  string id = 2;

  string name = 3;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 4;

  google.protobuf.Value for_each = 5 [json_name = "for_each"];

  repeated string depends_on = 6 [json_name = "depends_on"];

  int32 count = 7;

  string provider = 8;

  terraform.v1.Lifecycle lifecycle = 9;
}

// AwsKinesisStream version is 0
message AwsKinesisStream {
  string arn = 1;

  repeated string closed_shards = 2 [json_name = "closed_shards"];

  int64 creation_timestamp = 3 [json_name = "creation_timestamp"];

  string encryption_type = 4 [json_name = "encryption_type"];

  string id = 5;

  string kms_key_id = 6 [json_name = "kms_key_id"];

  int64 max_record_size_in_kib = 7 [json_name = "max_record_size_in_kib"];

  string name = 8;

  repeated string open_shards = 9 [json_name = "open_shards"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 10;

  int64 retention_period = 11 [json_name = "retention_period"];

  repeated string shard_level_metrics = 12 [json_name = "shard_level_metrics"];

  string status = 13;

  repeated StreamModeDetails stream_mode_details = 14 [json_name = "stream_mode_details"];

  map<string, string> tags = 15;

  google.protobuf.Value for_each = 16 [json_name = "for_each"];

  repeated string depends_on = 17 [json_name = "depends_on"];

  int32 count = 18;

  string provider = 19;

  terraform.v1.Lifecycle lifecycle = 20;

  message StreamModeDetails {
    // stream_mode: string
    string stream_mode = 1 [json_name = "stream_mode"];
  }
}

// AwsKinesisStreamConsumer version is 0
message AwsKinesisStreamConsumer {
  string arn = 1;

  string creation_timestamp = 2 [json_name = "creation_timestamp"];

  string id = 3;

  string name = 4;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 5;

  string status = 6;

  string stream_arn = 7 [json_name = "stream_arn"];

  map<string, string> tags = 8;

  google.protobuf.Value for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  int32 count = 11;

  string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;
}
