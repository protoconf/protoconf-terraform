syntax = "proto3";

// Provider: aws 6
package terraform.aws.datasources.v6;

import "google/protobuf/struct.proto";

import "terraform/v1/meta.proto";

// AwsServerlessapplicationrepositoryApplication version is 0
message AwsServerlessapplicationrepositoryApplication {
  string application_id = 1 [json_name = "application_id"];

  string id = 2;

  string name = 3;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 4;

  repeated string required_capabilities = 5 [json_name = "required_capabilities"];

  string semantic_version = 6 [json_name = "semantic_version"];

  string source_code_url = 7 [json_name = "source_code_url"];

  string template_url = 8 [json_name = "template_url"];

  google.protobuf.Value for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  int32 count = 11;

  string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;
}
