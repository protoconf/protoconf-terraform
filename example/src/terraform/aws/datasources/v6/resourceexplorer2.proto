syntax = "proto3";

// Provider: aws 6
package terraform.aws.datasources.v6;

import "google/protobuf/struct.proto";

import "terraform/v1/meta.proto";

// AwsResourceexplorer2Search version is 0
message AwsResourceexplorer2Search {
  string id = 1;

  string query_string = 2 [json_name = "query_string"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 3;

  repeated ResourceCount resource_count = 4 [json_name = "resource_count"];

  repeated Resources resources = 5;

  string view_arn = 6 [json_name = "view_arn"];

  google.protobuf.Value for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  int32 count = 9;

  string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;

  message ResourceCount {
    // complete: bool
    bool complete = 1;

    // total_resources: number
    int64 total_resources = 2 [json_name = "total_resources"];
  }

  message Resources {
    // arn: string
    string arn = 1;

    // last_reported_at: string
    string last_reported_at = 2 [json_name = "last_reported_at"];

    // owning_account_id: string
    string owning_account_id = 3 [json_name = "owning_account_id"];

    // properties: list of object
    repeated Properties properties = 4;

    // region: string
    string region = 5;

    // resource_type: string
    string resource_type = 6 [json_name = "resource_type"];

    // service: string
    string service = 7;

    message Properties {
      // data: string
      string data = 1;

      // last_reported_at: string
      string last_reported_at = 2 [json_name = "last_reported_at"];

      // name: string
      string name = 3;
    }
  }
}
