syntax = "proto3";

// Provider: aws 6
package terraform.aws.datasources.v6;

import "google/protobuf/struct.proto";

import "terraform/v1/meta.proto";

// AwsOamLink version is 0
message AwsOamLink {
  string arn = 1;

  string id = 2;

  string label = 3;

  string label_template = 4 [json_name = "label_template"];

  repeated LinkConfiguration link_configuration = 5 [json_name = "link_configuration"];

  string link_id = 6 [json_name = "link_id"];

  string link_identifier = 7 [json_name = "link_identifier"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 8;

  repeated string resource_types = 9 [json_name = "resource_types"];

  string sink_arn = 10 [json_name = "sink_arn"];

  map<string, string> tags = 11;

  google.protobuf.Value for_each = 12 [json_name = "for_each"];

  repeated string depends_on = 13 [json_name = "depends_on"];

  int32 count = 14;

  string provider = 15;

  terraform.v1.Lifecycle lifecycle = 16;

  message LinkConfiguration {
    // log_group_configuration: list of object
    repeated LogGroupConfiguration log_group_configuration = 1 [json_name = "log_group_configuration"];

    // metric_configuration: list of object
    repeated MetricConfiguration metric_configuration = 2 [json_name = "metric_configuration"];

    message LogGroupConfiguration {
      // filter: string
      string filter = 1;
    }

    message MetricConfiguration {
      // filter: string
      string filter = 1;
    }
  }
}

// AwsOamLinks version is 0
message AwsOamLinks {
  repeated string arns = 1;

  string id = 2;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 3;

  google.protobuf.Value for_each = 4 [json_name = "for_each"];

  repeated string depends_on = 5 [json_name = "depends_on"];

  int32 count = 6;

  string provider = 7;

  terraform.v1.Lifecycle lifecycle = 8;
}

// AwsOamSink version is 0
message AwsOamSink {
  string arn = 1;

  string id = 2;

  string name = 3;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 4;

  string sink_id = 5 [json_name = "sink_id"];

  string sink_identifier = 6 [json_name = "sink_identifier"];

  map<string, string> tags = 7;

  google.protobuf.Value for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  int32 count = 10;

  string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;
}

// AwsOamSinks version is 0
message AwsOamSinks {
  repeated string arns = 1;

  string id = 2;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 3;

  google.protobuf.Value for_each = 4 [json_name = "for_each"];

  repeated string depends_on = 5 [json_name = "depends_on"];

  int32 count = 6;

  string provider = 7;

  terraform.v1.Lifecycle lifecycle = 8;
}
