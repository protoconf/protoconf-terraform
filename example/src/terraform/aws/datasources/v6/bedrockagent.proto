syntax = "proto3";

// Provider: aws 6
package terraform.aws.datasources.v6;

import "google/protobuf/struct.proto";

import "terraform/v1/meta.proto";

// AwsBedrockagentAgentVersions version is 0
message AwsBedrockagentAgentVersions {
  string agent_id = 1 [json_name = "agent_id"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 2;

  repeated AgentVersionSummaries agent_version_summaries = 3 [json_name = "agent_version_summaries"];

  google.protobuf.Value for_each = 4 [json_name = "for_each"];

  repeated string depends_on = 5 [json_name = "depends_on"];

  int32 count = 6;

  string provider = 7;

  terraform.v1.Lifecycle lifecycle = 8;

  message AgentVersionSummaries {
    string agent_name = 1 [json_name = "agent_name"];

    string agent_status = 2 [json_name = "agent_status"];

    string agent_version = 3 [json_name = "agent_version"];

    string created_at = 4 [json_name = "created_at"];

    string description = 5;

    string updated_at = 6 [json_name = "updated_at"];

    repeated GuardrailConfiguration guardrail_configuration = 7 [json_name = "guardrail_configuration"];

    message GuardrailConfiguration {
      string guardrail_identifier = 1 [json_name = "guardrail_identifier"];

      string guardrail_version = 2 [json_name = "guardrail_version"];
    }
  }
}
