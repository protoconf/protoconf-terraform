syntax = "proto3";

// Provider: aws 6
package terraform.aws.datasources.v6;

import "google/protobuf/struct.proto";

import "terraform/v1/meta.proto";

// AwsPollyVoices version is 0
message AwsPollyVoices {
  string engine = 1;

  string id = 2;

  bool include_additional_language_codes = 3 [json_name = "include_additional_language_codes"];

  string language_code = 4 [json_name = "language_code"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 5;

  repeated Voices voices = 6;

  google.protobuf.Value for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  int32 count = 9;

  string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;

  message Voices {
    repeated string additional_language_codes = 1 [json_name = "additional_language_codes"];

    string gender = 2;

    string id = 3;

    string language_code = 4 [json_name = "language_code"];

    string language_name = 5 [json_name = "language_name"];

    string name = 6;

    repeated string supported_engines = 7 [json_name = "supported_engines"];
  }
}
