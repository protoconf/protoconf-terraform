syntax = "proto3";

// Provider: aws 6
package terraform.aws.datasources.v6;

import "google/protobuf/struct.proto";

import "terraform/v1/meta.proto";

// AwsSsoadminApplication version is 0
message AwsSsoadminApplication {
  string application_account = 1 [json_name = "application_account"];

  string application_arn = 2 [json_name = "application_arn"];

  string application_provider_arn = 3 [json_name = "application_provider_arn"];

  string description = 4;

  string id = 5;

  string instance_arn = 6 [json_name = "instance_arn"];

  string name = 7;

  repeated PortalOptions portal_options = 8 [json_name = "portal_options"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 9;

  string status = 10;

  google.protobuf.Value for_each = 11 [json_name = "for_each"];

  repeated string depends_on = 12 [json_name = "depends_on"];

  int32 count = 13;

  string provider = 14;

  terraform.v1.Lifecycle lifecycle = 15;

  message PortalOptions {
    // sign_in_options: list of object
    repeated SignInOptions sign_in_options = 1 [json_name = "sign_in_options"];

    // visibility: string
    string visibility = 2;

    message SignInOptions {
      // application_url: string
      string application_url = 1 [json_name = "application_url"];

      // origin: string
      string origin = 2;
    }
  }
}

// AwsSsoadminApplicationAssignments version is 0
message AwsSsoadminApplicationAssignments {
  string application_arn = 1 [json_name = "application_arn"];

  repeated ApplicationAssignments application_assignments = 2 [json_name = "application_assignments"];

  string id = 3;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 4;

  google.protobuf.Value for_each = 5 [json_name = "for_each"];

  repeated string depends_on = 6 [json_name = "depends_on"];

  int32 count = 7;

  string provider = 8;

  terraform.v1.Lifecycle lifecycle = 9;

  message ApplicationAssignments {
    // application_arn: string
    string application_arn = 1 [json_name = "application_arn"];

    // principal_id: string
    string principal_id = 2 [json_name = "principal_id"];

    // principal_type: string
    string principal_type = 3 [json_name = "principal_type"];
  }
}

// AwsSsoadminApplicationProviders version is 0
message AwsSsoadminApplicationProviders {
  repeated ApplicationProviders application_providers = 1 [json_name = "application_providers"];

  string id = 2;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 3;

  google.protobuf.Value for_each = 4 [json_name = "for_each"];

  repeated string depends_on = 5 [json_name = "depends_on"];

  int32 count = 6;

  string provider = 7;

  terraform.v1.Lifecycle lifecycle = 8;

  message ApplicationProviders {
    // application_provider_arn: string
    string application_provider_arn = 1 [json_name = "application_provider_arn"];

    // display_data: list of object
    repeated DisplayData display_data = 2 [json_name = "display_data"];

    // federation_protocol: string
    string federation_protocol = 3 [json_name = "federation_protocol"];

    message DisplayData {
      // description: string
      string description = 1;

      // display_name: string
      string display_name = 2 [json_name = "display_name"];

      // icon_url: string
      string icon_url = 3 [json_name = "icon_url"];
    }
  }
}

// AwsSsoadminInstances version is 0
message AwsSsoadminInstances {
  repeated string arns = 1;

  string id = 2;

  repeated string identity_store_ids = 3 [json_name = "identity_store_ids"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 4;

  google.protobuf.Value for_each = 5 [json_name = "for_each"];

  repeated string depends_on = 6 [json_name = "depends_on"];

  int32 count = 7;

  string provider = 8;

  terraform.v1.Lifecycle lifecycle = 9;
}

// AwsSsoadminPermissionSet version is 0
message AwsSsoadminPermissionSet {
  string arn = 1;

  string created_date = 2 [json_name = "created_date"];

  string description = 3;

  string id = 4;

  string instance_arn = 5 [json_name = "instance_arn"];

  string name = 6;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 7;

  string relay_state = 8 [json_name = "relay_state"];

  string session_duration = 9 [json_name = "session_duration"];

  map<string, string> tags = 10;

  google.protobuf.Value for_each = 11 [json_name = "for_each"];

  repeated string depends_on = 12 [json_name = "depends_on"];

  int32 count = 13;

  string provider = 14;

  terraform.v1.Lifecycle lifecycle = 15;
}

// AwsSsoadminPermissionSets version is 0
message AwsSsoadminPermissionSets {
  repeated string arns = 1;

  string id = 2;

  string instance_arn = 3 [json_name = "instance_arn"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 4;

  google.protobuf.Value for_each = 5 [json_name = "for_each"];

  repeated string depends_on = 6 [json_name = "depends_on"];

  int32 count = 7;

  string provider = 8;

  terraform.v1.Lifecycle lifecycle = 9;
}

// AwsSsoadminPrincipalApplicationAssignments version is 0
message AwsSsoadminPrincipalApplicationAssignments {
  string id = 1;

  string instance_arn = 2 [json_name = "instance_arn"];

  string principal_id = 3 [json_name = "principal_id"];

  string principal_type = 4 [json_name = "principal_type"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 5;

  repeated ApplicationAssignments application_assignments = 6 [json_name = "application_assignments"];

  google.protobuf.Value for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  int32 count = 9;

  string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;

  message ApplicationAssignments {
    string application_arn = 1 [json_name = "application_arn"];

    string principal_id = 2 [json_name = "principal_id"];

    string principal_type = 3 [json_name = "principal_type"];
  }
}
