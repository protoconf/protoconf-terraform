syntax = "proto3";

// Provider: aws 6
package terraform.aws.datasources.v6;

import "google/protobuf/struct.proto";

import "terraform/v1/meta.proto";

// AwsBackupFramework version is 0
message AwsBackupFramework {
  string arn = 1;

  repeated Control control = 2;

  string creation_time = 3 [json_name = "creation_time"];

  string deployment_status = 4 [json_name = "deployment_status"];

  string description = 5;

  string id = 6;

  string name = 7;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 8;

  string status = 9;

  map<string, string> tags = 10;

  google.protobuf.Value for_each = 11 [json_name = "for_each"];

  repeated string depends_on = 12 [json_name = "depends_on"];

  int32 count = 13;

  string provider = 14;

  terraform.v1.Lifecycle lifecycle = 15;

  message Control {
    // input_parameter: list of object
    repeated InputParameter input_parameter = 1 [json_name = "input_parameter"];

    // name: string
    string name = 2;

    // scope: list of object
    repeated Scope scope = 3;

    message InputParameter {
      // name: string
      string name = 1;

      // value: string
      string value = 2;
    }

    message Scope {
      // compliance_resource_ids: list of string
      repeated string compliance_resource_ids = 1 [json_name = "compliance_resource_ids"];

      // compliance_resource_types: list of string
      repeated string compliance_resource_types = 2 [json_name = "compliance_resource_types"];

      // tags: map of string
      map<string, string> tags = 3;
    }
  }
}

// AwsBackupPlan version is 0
message AwsBackupPlan {
  string arn = 1;

  string id = 2;

  string name = 3;

  string plan_id = 4 [json_name = "plan_id"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 5;

  repeated Rule rule = 6;

  repeated ScanSetting scan_setting = 7 [json_name = "scan_setting"];

  map<string, string> tags = 8;

  string version = 9;

  google.protobuf.Value for_each = 10 [json_name = "for_each"];

  repeated string depends_on = 11 [json_name = "depends_on"];

  int32 count = 12;

  string provider = 13;

  terraform.v1.Lifecycle lifecycle = 14;

  message Rule {
    // completion_window: number
    int64 completion_window = 1 [json_name = "completion_window"];

    // copy_action: set of object
    repeated CopyAction copy_action = 2 [json_name = "copy_action"];

    // enable_continuous_backup: bool
    bool enable_continuous_backup = 3 [json_name = "enable_continuous_backup"];

    // lifecycle: list of object
    repeated Lifecycle lifecycle = 4;

    // recovery_point_tags: map of string
    map<string, string> recovery_point_tags = 5 [json_name = "recovery_point_tags"];

    // rule_name: string
    string rule_name = 6 [json_name = "rule_name"];

    // scan_action: set of object
    repeated ScanAction scan_action = 7 [json_name = "scan_action"];

    // schedule: string
    string schedule = 8;

    // schedule_expression_timezone: string
    string schedule_expression_timezone = 9 [json_name = "schedule_expression_timezone"];

    // start_window: number
    int64 start_window = 10 [json_name = "start_window"];

    // target_logically_air_gapped_backup_vault_arn: string
    string target_logically_air_gapped_backup_vault_arn = 11 [
      json_name = "target_logically_air_gapped_backup_vault_arn"
    ];

    // target_vault_name: string
    string target_vault_name = 12 [json_name = "target_vault_name"];

    message CopyAction {
      // destination_vault_arn: string
      string destination_vault_arn = 1 [json_name = "destination_vault_arn"];

      // lifecycle: list of object
      repeated Lifecycle lifecycle = 2;

      message Lifecycle {
        // cold_storage_after: number
        int64 cold_storage_after = 1 [json_name = "cold_storage_after"];

        // delete_after: number
        int64 delete_after = 2 [json_name = "delete_after"];

        // opt_in_to_archive_for_supported_resources: bool
        bool opt_in_to_archive_for_supported_resources = 3 [
          json_name = "opt_in_to_archive_for_supported_resources"
        ];
      }
    }

    message Lifecycle {
      // cold_storage_after: number
      int64 cold_storage_after = 1 [json_name = "cold_storage_after"];

      // delete_after: number
      int64 delete_after = 2 [json_name = "delete_after"];

      // opt_in_to_archive_for_supported_resources: bool
      bool opt_in_to_archive_for_supported_resources = 3 [
        json_name = "opt_in_to_archive_for_supported_resources"
      ];
    }

    message ScanAction {
      // malware_scanner: string
      string malware_scanner = 1 [json_name = "malware_scanner"];

      // scan_mode: string
      string scan_mode = 2 [json_name = "scan_mode"];
    }
  }

  message ScanSetting {
    // malware_scanner: string
    string malware_scanner = 1 [json_name = "malware_scanner"];

    // resource_types: set of string
    repeated string resource_types = 2 [json_name = "resource_types"];

    // scanner_role_arn: string
    string scanner_role_arn = 3 [json_name = "scanner_role_arn"];
  }
}

// AwsBackupReportPlan version is 0
message AwsBackupReportPlan {
  string arn = 1;

  string creation_time = 2 [json_name = "creation_time"];

  string deployment_status = 3 [json_name = "deployment_status"];

  string description = 4;

  string id = 5;

  string name = 6;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 7;

  repeated ReportDeliveryChannel report_delivery_channel = 8 [json_name = "report_delivery_channel"];

  repeated ReportSetting report_setting = 9 [json_name = "report_setting"];

  map<string, string> tags = 10;

  google.protobuf.Value for_each = 11 [json_name = "for_each"];

  repeated string depends_on = 12 [json_name = "depends_on"];

  int32 count = 13;

  string provider = 14;

  terraform.v1.Lifecycle lifecycle = 15;

  message ReportDeliveryChannel {
    // formats: set of string
    repeated string formats = 1;

    // s3_bucket_name: string
    string s3_bucket_name = 2 [json_name = "s3_bucket_name"];

    // s3_key_prefix: string
    string s3_key_prefix = 3 [json_name = "s3_key_prefix"];
  }

  message ReportSetting {
    // accounts: set of string
    repeated string accounts = 1;

    // framework_arns: set of string
    repeated string framework_arns = 2 [json_name = "framework_arns"];

    // number_of_frameworks: number
    int64 number_of_frameworks = 3 [json_name = "number_of_frameworks"];

    // organization_units: set of string
    repeated string organization_units = 4 [json_name = "organization_units"];

    // regions: set of string
    repeated string regions = 5;

    // report_template: string
    string report_template = 6 [json_name = "report_template"];
  }
}

// AwsBackupSelection version is 0
message AwsBackupSelection {
  string iam_role_arn = 1 [json_name = "iam_role_arn"];

  string id = 2;

  string name = 3;

  string plan_id = 4 [json_name = "plan_id"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 5;

  repeated string resources = 6;

  string selection_id = 7 [json_name = "selection_id"];

  google.protobuf.Value for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  int32 count = 10;

  string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;
}

// AwsBackupVault version is 0
message AwsBackupVault {
  string arn = 1;

  string id = 2;

  string kms_key_arn = 3 [json_name = "kms_key_arn"];

  string name = 4;

  int64 recovery_points = 5 [json_name = "recovery_points"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 6;

  map<string, string> tags = 7;

  google.protobuf.Value for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  int32 count = 10;

  string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;
}
