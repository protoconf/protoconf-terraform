syntax = "proto3";

// Provider: aws 6
package terraform.aws.datasources.v6;

import "google/protobuf/struct.proto";

import "terraform/v1/meta.proto";

// AwsSsmcontactsContact version is 0
message AwsSsmcontactsContact {
  string alias = 1;

  string arn = 2;

  string display_name = 3 [json_name = "display_name"];

  string id = 4;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 5;

  map<string, string> tags = 6;

  string type = 7;

  google.protobuf.Value for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  int32 count = 10;

  string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;
}

// AwsSsmcontactsContactChannel version is 0
message AwsSsmcontactsContactChannel {
  string activation_status = 1 [json_name = "activation_status"];

  string arn = 2;

  string contact_id = 3 [json_name = "contact_id"];

  repeated DeliveryAddress delivery_address = 4 [json_name = "delivery_address"];

  string id = 5;

  string name = 6;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 7;

  string type = 8;

  google.protobuf.Value for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  int32 count = 11;

  string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message DeliveryAddress {
    // simple_address: string
    string simple_address = 1 [json_name = "simple_address"];
  }
}

// AwsSsmcontactsPlan version is 0
message AwsSsmcontactsPlan {
  string contact_id = 1 [json_name = "contact_id"];

  string id = 2;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 3;

  repeated Stage stage = 4;

  google.protobuf.Value for_each = 5 [json_name = "for_each"];

  repeated string depends_on = 6 [json_name = "depends_on"];

  int32 count = 7;

  string provider = 8;

  terraform.v1.Lifecycle lifecycle = 9;

  message Stage {
    // duration_in_minutes: number
    int64 duration_in_minutes = 1 [json_name = "duration_in_minutes"];

    // target: list of object
    repeated Target target = 2;

    message Target {
      // channel_target_info: list of object
      repeated ChannelTargetInfo channel_target_info = 1 [json_name = "channel_target_info"];

      // contact_target_info: list of object
      repeated ContactTargetInfo contact_target_info = 2 [json_name = "contact_target_info"];

      message ChannelTargetInfo {
        // contact_channel_id: string
        string contact_channel_id = 1 [json_name = "contact_channel_id"];

        // retry_interval_in_minutes: number
        int64 retry_interval_in_minutes = 2 [json_name = "retry_interval_in_minutes"];
      }

      message ContactTargetInfo {
        // contact_id: string
        string contact_id = 1 [json_name = "contact_id"];

        // is_essential: bool
        bool is_essential = 2 [json_name = "is_essential"];
      }
    }
  }
}

// AwsSsmcontactsRotation version is 0
message AwsSsmcontactsRotation {
  string arn = 1;

  repeated string contact_ids = 2 [json_name = "contact_ids"];

  string id = 3;

  string name = 4;

  repeated Recurrence recurrence = 5;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 6;

  string start_time = 7 [json_name = "start_time"];

  map<string, string> tags = 8;

  string time_zone_id = 9 [json_name = "time_zone_id"];

  google.protobuf.Value for_each = 10 [json_name = "for_each"];

  repeated string depends_on = 11 [json_name = "depends_on"];

  int32 count = 12;

  string provider = 13;

  terraform.v1.Lifecycle lifecycle = 14;

  message Recurrence {
    // daily_settings: list of object
    repeated DailySettings daily_settings = 1 [json_name = "daily_settings"];

    // monthly_settings: list of object
    repeated MonthlySettings monthly_settings = 2 [json_name = "monthly_settings"];

    // number_of_on_calls: number
    int64 number_of_on_calls = 3 [json_name = "number_of_on_calls"];

    // recurrence_multiplier: number
    int64 recurrence_multiplier = 4 [json_name = "recurrence_multiplier"];

    // shift_coverages: list of object
    repeated ShiftCoverages shift_coverages = 5 [json_name = "shift_coverages"];

    // weekly_settings: list of object
    repeated WeeklySettings weekly_settings = 6 [json_name = "weekly_settings"];

    message DailySettings {
      // hour_of_day: number
      int64 hour_of_day = 1 [json_name = "hour_of_day"];

      // minute_of_hour: number
      int64 minute_of_hour = 2 [json_name = "minute_of_hour"];
    }

    message MonthlySettings {
      // day_of_month: number
      int64 day_of_month = 1 [json_name = "day_of_month"];

      // hand_off_time: list of object
      repeated HandOffTime hand_off_time = 2 [json_name = "hand_off_time"];

      message HandOffTime {
        // hour_of_day: number
        int64 hour_of_day = 1 [json_name = "hour_of_day"];

        // minute_of_hour: number
        int64 minute_of_hour = 2 [json_name = "minute_of_hour"];
      }
    }

    message ShiftCoverages {
      // coverage_times: list of object
      repeated CoverageTimes coverage_times = 1 [json_name = "coverage_times"];

      // map_block_key: string
      string map_block_key = 2 [json_name = "map_block_key"];

      message CoverageTimes {
        // end: list of object
        repeated End end = 1;

        // start: list of object
        repeated Start start = 2;

        message End {
          // hour_of_day: number
          int64 hour_of_day = 1 [json_name = "hour_of_day"];

          // minute_of_hour: number
          int64 minute_of_hour = 2 [json_name = "minute_of_hour"];
        }

        message Start {
          // hour_of_day: number
          int64 hour_of_day = 1 [json_name = "hour_of_day"];

          // minute_of_hour: number
          int64 minute_of_hour = 2 [json_name = "minute_of_hour"];
        }
      }
    }

    message WeeklySettings {
      // day_of_week: string
      string day_of_week = 1 [json_name = "day_of_week"];

      // hand_off_time: list of object
      repeated HandOffTime hand_off_time = 2 [json_name = "hand_off_time"];

      message HandOffTime {
        // hour_of_day: number
        int64 hour_of_day = 1 [json_name = "hour_of_day"];

        // minute_of_hour: number
        int64 minute_of_hour = 2 [json_name = "minute_of_hour"];
      }
    }
  }
}
