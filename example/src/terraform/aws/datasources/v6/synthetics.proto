syntax = "proto3";

// Provider: aws 6
package terraform.aws.datasources.v6;

import "google/protobuf/struct.proto";

import "terraform/v1/meta.proto";

// AwsSyntheticsRuntimeVersion version is 0
message AwsSyntheticsRuntimeVersion {
  string deprecation_date = 1 [json_name = "deprecation_date"];

  string description = 2;

  string id = 3;

  bool latest = 4;

  string prefix = 5;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 6;

  string release_date = 7 [json_name = "release_date"];

  string version = 8;

  string version_name = 9 [json_name = "version_name"];

  google.protobuf.Value for_each = 10 [json_name = "for_each"];

  repeated string depends_on = 11 [json_name = "depends_on"];

  int32 count = 12;

  string provider = 13;

  terraform.v1.Lifecycle lifecycle = 14;
}

// AwsSyntheticsRuntimeVersions version is 0
message AwsSyntheticsRuntimeVersions {
  string id = 1;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 2;

  repeated RuntimeVersions runtime_versions = 3 [json_name = "runtime_versions"];

  google.protobuf.Value for_each = 4 [json_name = "for_each"];

  repeated string depends_on = 5 [json_name = "depends_on"];

  int32 count = 6;

  string provider = 7;

  terraform.v1.Lifecycle lifecycle = 8;

  message RuntimeVersions {
    // deprecation_date: string
    string deprecation_date = 1 [json_name = "deprecation_date"];

    // description: string
    string description = 2;

    // release_date: string
    string release_date = 3 [json_name = "release_date"];

    // version_name: string
    string version_name = 4 [json_name = "version_name"];
  }
}
