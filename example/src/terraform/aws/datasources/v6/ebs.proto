syntax = "proto3";

// Provider: aws 6
package terraform.aws.datasources.v6;

import "google/protobuf/struct.proto";

import "terraform/v1/meta.proto";

// AwsEbsDefaultKmsKey version is 0
message AwsEbsDefaultKmsKey {
  string id = 1;

  string key_arn = 2 [json_name = "key_arn"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 3;

  repeated Timeouts timeouts = 4;

  google.protobuf.Value for_each = 5 [json_name = "for_each"];

  repeated string depends_on = 6 [json_name = "depends_on"];

  int32 count = 7;

  string provider = 8;

  terraform.v1.Lifecycle lifecycle = 9;

  message Timeouts {
    string read = 1;
  }
}

// AwsEbsEncryptionByDefault version is 0
message AwsEbsEncryptionByDefault {
  bool enabled = 1;

  string id = 2;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 3;

  repeated Timeouts timeouts = 4;

  google.protobuf.Value for_each = 5 [json_name = "for_each"];

  repeated string depends_on = 6 [json_name = "depends_on"];

  int32 count = 7;

  string provider = 8;

  terraform.v1.Lifecycle lifecycle = 9;

  message Timeouts {
    string read = 1;
  }
}

// AwsEbsSnapshot version is 0
message AwsEbsSnapshot {
  string arn = 1;

  string data_encryption_key_id = 2 [json_name = "data_encryption_key_id"];

  string description = 3;

  bool encrypted = 4;

  string id = 5;

  string kms_key_id = 6 [json_name = "kms_key_id"];

  bool most_recent = 7 [json_name = "most_recent"];

  string outpost_arn = 8 [json_name = "outpost_arn"];

  string owner_alias = 9 [json_name = "owner_alias"];

  string owner_id = 10 [json_name = "owner_id"];

  repeated string owners = 11;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 12;

  repeated string restorable_by_user_ids = 13 [json_name = "restorable_by_user_ids"];

  string snapshot_id = 14 [json_name = "snapshot_id"];

  repeated string snapshot_ids = 15 [json_name = "snapshot_ids"];

  string start_time = 16 [json_name = "start_time"];

  string state = 17;

  string storage_tier = 18 [json_name = "storage_tier"];

  map<string, string> tags = 19;

  string volume_id = 20 [json_name = "volume_id"];

  int64 volume_size = 21 [json_name = "volume_size"];

  repeated Filter filter = 22;

  repeated Timeouts timeouts = 23;

  google.protobuf.Value for_each = 24 [json_name = "for_each"];

  repeated string depends_on = 25 [json_name = "depends_on"];

  int32 count = 26;

  string provider = 27;

  terraform.v1.Lifecycle lifecycle = 28;

  message Filter {
    string name = 1;

    repeated string values = 2;
  }

  message Timeouts {
    string read = 1;
  }
}

// AwsEbsSnapshotIds version is 0
message AwsEbsSnapshotIds {
  string id = 1;

  repeated string ids = 2;

  repeated string owners = 3;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 4;

  repeated string restorable_by_user_ids = 5 [json_name = "restorable_by_user_ids"];

  repeated Filter filter = 6;

  repeated Timeouts timeouts = 7;

  google.protobuf.Value for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  int32 count = 10;

  string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;

  message Filter {
    string name = 1;

    repeated string values = 2;
  }

  message Timeouts {
    string read = 1;
  }
}

// AwsEbsVolume version is 0
message AwsEbsVolume {
  string arn = 1;

  string availability_zone = 2 [json_name = "availability_zone"];

  string create_time = 3 [json_name = "create_time"];

  bool encrypted = 4;

  string id = 5;

  int64 iops = 6;

  string kms_key_id = 7 [json_name = "kms_key_id"];

  bool most_recent = 8 [json_name = "most_recent"];

  bool multi_attach_enabled = 9 [json_name = "multi_attach_enabled"];

  string outpost_arn = 10 [json_name = "outpost_arn"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 11;

  int64 size = 12;

  string snapshot_id = 13 [json_name = "snapshot_id"];

  map<string, string> tags = 14;

  int64 throughput = 15;

  string volume_id = 16 [json_name = "volume_id"];

  int64 volume_initialization_rate = 17 [json_name = "volume_initialization_rate"];

  string volume_type = 18 [json_name = "volume_type"];

  repeated Filter filter = 19;

  repeated Timeouts timeouts = 20;

  google.protobuf.Value for_each = 21 [json_name = "for_each"];

  repeated string depends_on = 22 [json_name = "depends_on"];

  int32 count = 23;

  string provider = 24;

  terraform.v1.Lifecycle lifecycle = 25;

  message Filter {
    string name = 1;

    repeated string values = 2;
  }

  message Timeouts {
    string read = 1;
  }
}

// AwsEbsVolumes version is 0
message AwsEbsVolumes {
  string id = 1;

  repeated string ids = 2;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 3;

  map<string, string> tags = 4;

  repeated Filter filter = 5;

  repeated Timeouts timeouts = 6;

  google.protobuf.Value for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  int32 count = 9;

  string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;

  message Filter {
    string name = 1;

    repeated string values = 2;
  }

  message Timeouts {
    string read = 1;
  }
}
