syntax = "proto3";

// Provider: aws 6
package terraform.aws.resources.v6;

import "google/protobuf/struct.proto";

import "terraform/v1/meta.proto";

// AwsDatapipelinePipeline version is 0
message AwsDatapipelinePipeline {
  string description = 1;

  string id = 2;

  string name = 3;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 4;

  map<string, string> tags = 5;

  map<string, string> tags_all = 6 [json_name = "tags_all"];

  google.protobuf.Value for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  int32 count = 9;

  string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;
}

// AwsDatapipelinePipelineDefinition version is 0
message AwsDatapipelinePipelineDefinition {
  string id = 1;

  string pipeline_id = 2 [json_name = "pipeline_id"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 3;

  repeated ParameterObject parameter_object = 4 [json_name = "parameter_object"];

  repeated ParameterValue parameter_value = 5 [json_name = "parameter_value"];

  repeated PipelineObject pipeline_object = 6 [json_name = "pipeline_object"];

  google.protobuf.Value for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  int32 count = 9;

  string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;

  message ParameterObject {
    string id = 1;

    repeated Attribute attribute = 2;

    message Attribute {
      string key = 1;

      string string_value = 2 [json_name = "string_value"];
    }
  }

  message ParameterValue {
    string id = 1;

    string string_value = 2 [json_name = "string_value"];
  }

  message PipelineObject {
    string id = 1;

    string name = 2;

    repeated Field field = 3;

    message Field {
      string key = 1;

      string ref_value = 2 [json_name = "ref_value"];

      string string_value = 3 [json_name = "string_value"];
    }
  }
}
