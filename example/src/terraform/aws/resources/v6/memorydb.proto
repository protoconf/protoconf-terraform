syntax = "proto3";

// Provider: aws 6
package terraform.aws.resources.v6;

import "google/protobuf/struct.proto";

import "terraform/v1/meta.proto";

// AwsMemorydbAcl version is 0
message AwsMemorydbAcl {
  string arn = 1;

  string id = 2;

  string minimum_engine_version = 3 [json_name = "minimum_engine_version"];

  string name = 4;

  string name_prefix = 5 [json_name = "name_prefix"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 6;

  map<string, string> tags = 7;

  map<string, string> tags_all = 8 [json_name = "tags_all"];

  repeated string user_names = 9 [json_name = "user_names"];

  google.protobuf.Value for_each = 10 [json_name = "for_each"];

  repeated string depends_on = 11 [json_name = "depends_on"];

  int32 count = 12;

  string provider = 13;

  terraform.v1.Lifecycle lifecycle = 14;
}

// AwsMemorydbCluster version is 0
message AwsMemorydbCluster {
  string acl_name = 1 [json_name = "acl_name"];

  string arn = 2;

  bool auto_minor_version_upgrade = 3 [json_name = "auto_minor_version_upgrade"];

  repeated ClusterEndpoint cluster_endpoint = 4 [json_name = "cluster_endpoint"];

  bool data_tiering = 5 [json_name = "data_tiering"];

  string description = 6;

  string engine = 7;

  string engine_patch_version = 8 [json_name = "engine_patch_version"];

  string engine_version = 9 [json_name = "engine_version"];

  string final_snapshot_name = 10 [json_name = "final_snapshot_name"];

  string id = 11;

  string ip_discovery = 12 [json_name = "ip_discovery"];

  string kms_key_arn = 13 [json_name = "kms_key_arn"];

  string maintenance_window = 14 [json_name = "maintenance_window"];

  string multi_region_cluster_name = 15 [json_name = "multi_region_cluster_name"];

  string name = 16;

  string name_prefix = 17 [json_name = "name_prefix"];

  string network_type = 18 [json_name = "network_type"];

  string node_type = 19 [json_name = "node_type"];

  int64 num_replicas_per_shard = 20 [json_name = "num_replicas_per_shard"];

  int64 num_shards = 21 [json_name = "num_shards"];

  string parameter_group_name = 22 [json_name = "parameter_group_name"];

  int64 port = 23;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 24;

  repeated string security_group_ids = 25 [json_name = "security_group_ids"];

  repeated Shards shards = 26;

  repeated string snapshot_arns = 27 [json_name = "snapshot_arns"];

  string snapshot_name = 28 [json_name = "snapshot_name"];

  int64 snapshot_retention_limit = 29 [json_name = "snapshot_retention_limit"];

  string snapshot_window = 30 [json_name = "snapshot_window"];

  string sns_topic_arn = 31 [json_name = "sns_topic_arn"];

  string subnet_group_name = 32 [json_name = "subnet_group_name"];

  map<string, string> tags = 33;

  map<string, string> tags_all = 34 [json_name = "tags_all"];

  bool tls_enabled = 35 [json_name = "tls_enabled"];

  repeated Timeouts timeouts = 36;

  google.protobuf.Value for_each = 37 [json_name = "for_each"];

  repeated string depends_on = 38 [json_name = "depends_on"];

  int32 count = 39;

  string provider = 40;

  terraform.v1.Lifecycle lifecycle = 41;

  message ClusterEndpoint {
    // address: string
    string address = 1;

    // port: number
    int64 port = 2;
  }

  message Shards {
    // name: string
    string name = 1;

    // nodes: set of object
    repeated Nodes nodes = 2;

    // num_nodes: number
    int64 num_nodes = 3 [json_name = "num_nodes"];

    // slots: string
    string slots = 4;

    message Nodes {
      // availability_zone: string
      string availability_zone = 1 [json_name = "availability_zone"];

      // create_time: string
      string create_time = 2 [json_name = "create_time"];

      // endpoint: list of object
      repeated Endpoint endpoint = 3;

      // name: string
      string name = 4;

      message Endpoint {
        // address: string
        string address = 1;

        // port: number
        int64 port = 2;
      }
    }
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// AwsMemorydbMultiRegionCluster version is 0
message AwsMemorydbMultiRegionCluster {
  string arn = 1;

  string description = 2;

  string engine = 3;

  string engine_version = 4 [json_name = "engine_version"];

  string multi_region_cluster_name = 5 [json_name = "multi_region_cluster_name"];

  string multi_region_cluster_name_suffix = 6 [json_name = "multi_region_cluster_name_suffix"];

  string multi_region_parameter_group_name = 7 [json_name = "multi_region_parameter_group_name"];

  string node_type = 8 [json_name = "node_type"];

  int64 num_shards = 9 [json_name = "num_shards"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 10;

  string status = 11;

  map<string, string> tags = 12;

  map<string, string> tags_all = 13 [json_name = "tags_all"];

  bool tls_enabled = 14 [json_name = "tls_enabled"];

  string update_strategy = 15 [json_name = "update_strategy"];

  repeated Timeouts timeouts = 16;

  google.protobuf.Value for_each = 17 [json_name = "for_each"];

  repeated string depends_on = 18 [json_name = "depends_on"];

  int32 count = 19;

  string provider = 20;

  terraform.v1.Lifecycle lifecycle = 21;

  message Timeouts {
    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours).
    string create = 1;

    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours). Setting a timeout for a Delete operation is only
    // applicable if changes are saved into state before the destroy operation occurs.
    string delete = 2;

    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours).
    string update = 3;
  }
}

// AwsMemorydbParameterGroup version is 0
message AwsMemorydbParameterGroup {
  string arn = 1;

  string description = 2;

  string family = 3;

  string id = 4;

  string name = 5;

  string name_prefix = 6 [json_name = "name_prefix"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 7;

  map<string, string> tags = 8;

  map<string, string> tags_all = 9 [json_name = "tags_all"];

  repeated Parameter parameter = 10;

  google.protobuf.Value for_each = 11 [json_name = "for_each"];

  repeated string depends_on = 12 [json_name = "depends_on"];

  int32 count = 13;

  string provider = 14;

  terraform.v1.Lifecycle lifecycle = 15;

  message Parameter {
    string name = 1;

    string value = 2;
  }
}

// AwsMemorydbSnapshot version is 0
message AwsMemorydbSnapshot {
  string arn = 1;

  repeated ClusterConfiguration cluster_configuration = 2 [json_name = "cluster_configuration"];

  string cluster_name = 3 [json_name = "cluster_name"];

  string id = 4;

  string kms_key_arn = 5 [json_name = "kms_key_arn"];

  string name = 6;

  string name_prefix = 7 [json_name = "name_prefix"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 8;

  string source = 9;

  map<string, string> tags = 10;

  map<string, string> tags_all = 11 [json_name = "tags_all"];

  repeated Timeouts timeouts = 12;

  google.protobuf.Value for_each = 13 [json_name = "for_each"];

  repeated string depends_on = 14 [json_name = "depends_on"];

  int32 count = 15;

  string provider = 16;

  terraform.v1.Lifecycle lifecycle = 17;

  message ClusterConfiguration {
    // description: string
    string description = 1;

    // engine: string
    string engine = 2;

    // engine_version: string
    string engine_version = 3 [json_name = "engine_version"];

    // maintenance_window: string
    string maintenance_window = 4 [json_name = "maintenance_window"];

    // name: string
    string name = 5;

    // node_type: string
    string node_type = 6 [json_name = "node_type"];

    // num_shards: number
    int64 num_shards = 7 [json_name = "num_shards"];

    // parameter_group_name: string
    string parameter_group_name = 8 [json_name = "parameter_group_name"];

    // port: number
    int64 port = 9;

    // snapshot_retention_limit: number
    int64 snapshot_retention_limit = 10 [json_name = "snapshot_retention_limit"];

    // snapshot_window: string
    string snapshot_window = 11 [json_name = "snapshot_window"];

    // subnet_group_name: string
    string subnet_group_name = 12 [json_name = "subnet_group_name"];

    // topic_arn: string
    string topic_arn = 13 [json_name = "topic_arn"];

    // vpc_id: string
    string vpc_id = 14 [json_name = "vpc_id"];
  }

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}

// AwsMemorydbSubnetGroup version is 0
message AwsMemorydbSubnetGroup {
  string arn = 1;

  string description = 2;

  string id = 3;

  string name = 4;

  string name_prefix = 5 [json_name = "name_prefix"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 6;

  repeated string subnet_ids = 7 [json_name = "subnet_ids"];

  map<string, string> tags = 8;

  map<string, string> tags_all = 9 [json_name = "tags_all"];

  string vpc_id = 10 [json_name = "vpc_id"];

  google.protobuf.Value for_each = 11 [json_name = "for_each"];

  repeated string depends_on = 12 [json_name = "depends_on"];

  int32 count = 13;

  string provider = 14;

  terraform.v1.Lifecycle lifecycle = 15;
}

// AwsMemorydbUser version is 0
message AwsMemorydbUser {
  string access_string = 1 [json_name = "access_string"];

  string arn = 2;

  string id = 3;

  string minimum_engine_version = 4 [json_name = "minimum_engine_version"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 5;

  map<string, string> tags = 6;

  map<string, string> tags_all = 7 [json_name = "tags_all"];

  string user_name = 8 [json_name = "user_name"];

  AuthenticationMode authentication_mode = 9 [json_name = "authentication_mode"];

  google.protobuf.Value for_each = 10 [json_name = "for_each"];

  repeated string depends_on = 11 [json_name = "depends_on"];

  int32 count = 12;

  string provider = 13;

  terraform.v1.Lifecycle lifecycle = 14;

  message AuthenticationMode {
    int64 password_count = 1 [json_name = "password_count"];

    repeated string passwords = 2;

    string type = 3;
  }
}
