syntax = "proto3";

// Provider: aws 6
package terraform.aws.resources.v6;

import "google/protobuf/struct.proto";

import "terraform/v1/meta.proto";

// AwsSignerSigningJob version is 0
message AwsSignerSigningJob {
  string completed_at = 1 [json_name = "completed_at"];

  string created_at = 2 [json_name = "created_at"];

  string id = 3;

  bool ignore_signing_job_failure = 4 [json_name = "ignore_signing_job_failure"];

  string job_id = 5 [json_name = "job_id"];

  string job_invoker = 6 [json_name = "job_invoker"];

  string job_owner = 7 [json_name = "job_owner"];

  string platform_display_name = 8 [json_name = "platform_display_name"];

  string platform_id = 9 [json_name = "platform_id"];

  string profile_name = 10 [json_name = "profile_name"];

  string profile_version = 11 [json_name = "profile_version"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 12;

  string requested_by = 13 [json_name = "requested_by"];

  repeated RevocationRecord revocation_record = 14 [json_name = "revocation_record"];

  string signature_expires_at = 15 [json_name = "signature_expires_at"];

  repeated SignedObject signed_object = 16 [json_name = "signed_object"];

  string status = 17;

  string status_reason = 18 [json_name = "status_reason"];

  Destination destination = 19;

  Source source = 20;

  google.protobuf.Value for_each = 21 [json_name = "for_each"];

  repeated string depends_on = 22 [json_name = "depends_on"];

  int32 count = 23;

  string provider = 24;

  terraform.v1.Lifecycle lifecycle = 25;

  message RevocationRecord {
    // reason: string
    string reason = 1;

    // revoked_at: string
    string revoked_at = 2 [json_name = "revoked_at"];

    // revoked_by: string
    string revoked_by = 3 [json_name = "revoked_by"];
  }

  message SignedObject {
    // s3: list of object
    repeated S3 s3 = 1;

    message S3 {
      // bucket: string
      string bucket = 1;

      // key: string
      string key = 2;
    }
  }

  message Destination {
    S3 s3 = 1;

    message S3 {
      string bucket = 1;

      string prefix = 2;
    }
  }

  message Source {
    S3 s3 = 1;

    message S3 {
      string bucket = 1;

      string key = 2;

      string version = 3;
    }
  }
}

// AwsSignerSigningProfile version is 0
message AwsSignerSigningProfile {
  string arn = 1;

  string id = 2;

  string name = 3;

  string name_prefix = 4 [json_name = "name_prefix"];

  string platform_display_name = 5 [json_name = "platform_display_name"];

  string platform_id = 6 [json_name = "platform_id"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 7;

  repeated RevocationRecord revocation_record = 8 [json_name = "revocation_record"];

  map<string, string> signing_parameters = 9 [json_name = "signing_parameters"];

  string status = 10;

  map<string, string> tags = 11;

  map<string, string> tags_all = 12 [json_name = "tags_all"];

  string version = 13;

  string version_arn = 14 [json_name = "version_arn"];

  SignatureValidityPeriod signature_validity_period = 15 [json_name = "signature_validity_period"];

  SigningMaterial signing_material = 16 [json_name = "signing_material"];

  google.protobuf.Value for_each = 17 [json_name = "for_each"];

  repeated string depends_on = 18 [json_name = "depends_on"];

  int32 count = 19;

  string provider = 20;

  terraform.v1.Lifecycle lifecycle = 21;

  message RevocationRecord {
    // revocation_effective_from: string
    string revocation_effective_from = 1 [json_name = "revocation_effective_from"];

    // revoked_at: string
    string revoked_at = 2 [json_name = "revoked_at"];

    // revoked_by: string
    string revoked_by = 3 [json_name = "revoked_by"];
  }

  message SignatureValidityPeriod {
    string type = 1;

    int64 value = 2;
  }

  message SigningMaterial {
    string certificate_arn = 1 [json_name = "certificate_arn"];
  }
}

// AwsSignerSigningProfilePermission version is 0
message AwsSignerSigningProfilePermission {
  string action = 1;

  string id = 2;

  string principal = 3;

  string profile_name = 4 [json_name = "profile_name"];

  string profile_version = 5 [json_name = "profile_version"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 6;

  string statement_id = 7 [json_name = "statement_id"];

  string statement_id_prefix = 8 [json_name = "statement_id_prefix"];

  google.protobuf.Value for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  int32 count = 11;

  string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;
}
