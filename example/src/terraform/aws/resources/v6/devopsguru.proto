syntax = "proto3";

// Provider: aws 6
package terraform.aws.resources.v6;

import "google/protobuf/struct.proto";

import "terraform/v1/meta.proto";

// AwsDevopsguruEventSourcesConfig version is 0
message AwsDevopsguruEventSourcesConfig {
  string id = 1;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 2;

  repeated EventSources event_sources = 3 [json_name = "event_sources"];

  google.protobuf.Value for_each = 4 [json_name = "for_each"];

  repeated string depends_on = 5 [json_name = "depends_on"];

  int32 count = 6;

  string provider = 7;

  terraform.v1.Lifecycle lifecycle = 8;

  message EventSources {
    repeated AmazonCodeGuruProfiler amazon_code_guru_profiler = 1 [json_name = "amazon_code_guru_profiler"];

    message AmazonCodeGuruProfiler {
      string status = 1;
    }
  }
}

// AwsDevopsguruNotificationChannel version is 0
message AwsDevopsguruNotificationChannel {
  string id = 1;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 2;

  repeated Filters filters = 3;

  repeated Sns sns = 4;

  google.protobuf.Value for_each = 5 [json_name = "for_each"];

  repeated string depends_on = 6 [json_name = "depends_on"];

  int32 count = 7;

  string provider = 8;

  terraform.v1.Lifecycle lifecycle = 9;

  message Filters {
    repeated string message_types = 1 [json_name = "message_types"];

    repeated string severities = 2;
  }

  message Sns {
    string topic_arn = 1 [json_name = "topic_arn"];
  }
}

// AwsDevopsguruResourceCollection version is 0
message AwsDevopsguruResourceCollection {
  string id = 1;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 2;

  string type = 3;

  repeated Cloudformation cloudformation = 4;

  repeated Tags tags = 5;

  google.protobuf.Value for_each = 6 [json_name = "for_each"];

  repeated string depends_on = 7 [json_name = "depends_on"];

  int32 count = 8;

  string provider = 9;

  terraform.v1.Lifecycle lifecycle = 10;

  message Cloudformation {
    repeated string stack_names = 1 [json_name = "stack_names"];
  }

  message Tags {
    string app_boundary_key = 1 [json_name = "app_boundary_key"];

    repeated string tag_values = 2 [json_name = "tag_values"];
  }
}

// AwsDevopsguruServiceIntegration version is 0
message AwsDevopsguruServiceIntegration {
  string id = 1;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 2;

  repeated KmsServerSideEncryption kms_server_side_encryption = 3 [json_name = "kms_server_side_encryption"];

  repeated LogsAnomalyDetection logs_anomaly_detection = 4 [json_name = "logs_anomaly_detection"];

  repeated OpsCenter ops_center = 5 [json_name = "ops_center"];

  google.protobuf.Value for_each = 6 [json_name = "for_each"];

  repeated string depends_on = 7 [json_name = "depends_on"];

  int32 count = 8;

  string provider = 9;

  terraform.v1.Lifecycle lifecycle = 10;

  message KmsServerSideEncryption {
    string kms_key_id = 1 [json_name = "kms_key_id"];

    string opt_in_status = 2 [json_name = "opt_in_status"];

    string type = 3;
  }

  message LogsAnomalyDetection {
    string opt_in_status = 1 [json_name = "opt_in_status"];
  }

  message OpsCenter {
    string opt_in_status = 1 [json_name = "opt_in_status"];
  }
}
