syntax = "proto3";

// Provider: aws 6
package terraform.aws.resources.v6;

import "google/protobuf/struct.proto";

import "terraform/v1/meta.proto";

// AwsNetworkmonitorMonitor version is 0
message AwsNetworkmonitorMonitor {
  int64 aggregation_period = 1 [json_name = "aggregation_period"];

  string arn = 2;

  string id = 3;

  string monitor_name = 4 [json_name = "monitor_name"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 5;

  map<string, string> tags = 6;

  map<string, string> tags_all = 7 [json_name = "tags_all"];

  google.protobuf.Value for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  int32 count = 10;

  string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;
}

// AwsNetworkmonitorProbe version is 0
message AwsNetworkmonitorProbe {
  string address_family = 1 [json_name = "address_family"];

  string arn = 2;

  string destination = 3;

  int64 destination_port = 4 [json_name = "destination_port"];

  string id = 5;

  string monitor_name = 6 [json_name = "monitor_name"];

  int64 packet_size = 7 [json_name = "packet_size"];

  string probe_id = 8 [json_name = "probe_id"];

  string protocol = 9;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 10;

  string source_arn = 11 [json_name = "source_arn"];

  map<string, string> tags = 12;

  map<string, string> tags_all = 13 [json_name = "tags_all"];

  string vpc_id = 14 [json_name = "vpc_id"];

  google.protobuf.Value for_each = 15 [json_name = "for_each"];

  repeated string depends_on = 16 [json_name = "depends_on"];

  int32 count = 17;

  string provider = 18;

  terraform.v1.Lifecycle lifecycle = 19;
}
