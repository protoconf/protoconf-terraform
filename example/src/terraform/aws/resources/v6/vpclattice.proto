syntax = "proto3";

// Provider: aws 6
package terraform.aws.resources.v6;

import "google/protobuf/struct.proto";

import "terraform/v1/meta.proto";

// AwsVpclatticeAccessLogSubscription version is 0
message AwsVpclatticeAccessLogSubscription {
  string arn = 1;

  string destination_arn = 2 [json_name = "destination_arn"];

  string id = 3;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 4;

  string resource_arn = 5 [json_name = "resource_arn"];

  string resource_identifier = 6 [json_name = "resource_identifier"];

  string service_network_log_type = 7 [json_name = "service_network_log_type"];

  map<string, string> tags = 8;

  map<string, string> tags_all = 9 [json_name = "tags_all"];

  google.protobuf.Value for_each = 10 [json_name = "for_each"];

  repeated string depends_on = 11 [json_name = "depends_on"];

  int32 count = 12;

  string provider = 13;

  terraform.v1.Lifecycle lifecycle = 14;
}

// AwsVpclatticeAuthPolicy version is 0
message AwsVpclatticeAuthPolicy {
  string id = 1;

  string policy = 2;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 3;

  string resource_identifier = 4 [json_name = "resource_identifier"];

  string state = 5;

  repeated Timeouts timeouts = 6;

  google.protobuf.Value for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  int32 count = 9;

  string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// AwsVpclatticeDomainVerification version is 0
message AwsVpclatticeDomainVerification {
  string arn = 1;

  string created_at = 2 [json_name = "created_at"];

  string domain_name = 3 [json_name = "domain_name"];

  string id = 4;

  string last_verified_time = 5 [json_name = "last_verified_time"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 6;

  string status = 7;

  map<string, string> tags = 8;

  map<string, string> tags_all = 9 [json_name = "tags_all"];

  string txt_record_name = 10 [json_name = "txt_record_name"];

  string txt_record_value = 11 [json_name = "txt_record_value"];

  google.protobuf.Value for_each = 12 [json_name = "for_each"];

  repeated string depends_on = 13 [json_name = "depends_on"];

  int32 count = 14;

  string provider = 15;

  terraform.v1.Lifecycle lifecycle = 16;
}

// AwsVpclatticeListener version is 0
message AwsVpclatticeListener {
  string arn = 1;

  string created_at = 2 [json_name = "created_at"];

  string id = 3;

  string last_updated_at = 4 [json_name = "last_updated_at"];

  string listener_id = 5 [json_name = "listener_id"];

  string name = 6;

  int64 port = 7;

  string protocol = 8;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 9;

  string service_arn = 10 [json_name = "service_arn"];

  string service_identifier = 11 [json_name = "service_identifier"];

  map<string, string> tags = 12;

  map<string, string> tags_all = 13 [json_name = "tags_all"];

  DefaultAction default_action = 14 [json_name = "default_action"];

  repeated Timeouts timeouts = 15;

  google.protobuf.Value for_each = 16 [json_name = "for_each"];

  repeated string depends_on = 17 [json_name = "depends_on"];

  int32 count = 18;

  string provider = 19;

  terraform.v1.Lifecycle lifecycle = 20;

  message DefaultAction {
    FixedResponse fixed_response = 1 [json_name = "fixed_response"];

    repeated Forward forward = 2;

    message FixedResponse {
      int64 status_code = 1 [json_name = "status_code"];
    }

    message Forward {
      repeated TargetGroups target_groups = 1 [json_name = "target_groups"];

      message TargetGroups {
        string target_group_identifier = 1 [json_name = "target_group_identifier"];

        int64 weight = 2;
      }
    }
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// AwsVpclatticeListenerRule version is 0
message AwsVpclatticeListenerRule {
  string arn = 1;

  string id = 2;

  string listener_identifier = 3 [json_name = "listener_identifier"];

  string name = 4;

  int64 priority = 5;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 6;

  string rule_id = 7 [json_name = "rule_id"];

  string service_identifier = 8 [json_name = "service_identifier"];

  map<string, string> tags = 9;

  map<string, string> tags_all = 10 [json_name = "tags_all"];

  Action action = 11;

  Match match = 12;

  repeated Timeouts timeouts = 13;

  google.protobuf.Value for_each = 14 [json_name = "for_each"];

  repeated string depends_on = 15 [json_name = "depends_on"];

  int32 count = 16;

  string provider = 17;

  terraform.v1.Lifecycle lifecycle = 18;

  message Action {
    FixedResponse fixed_response = 1 [json_name = "fixed_response"];

    Forward forward = 2;

    message FixedResponse {
      int64 status_code = 1 [json_name = "status_code"];
    }

    message Forward {
      repeated TargetGroups target_groups = 1 [json_name = "target_groups"];

      message TargetGroups {
        string target_group_identifier = 1 [json_name = "target_group_identifier"];

        int64 weight = 2;
      }
    }
  }

  message Match {
    HttpMatch http_match = 1 [json_name = "http_match"];

    message HttpMatch {
      string method = 1;

      repeated HeaderMatches header_matches = 2 [json_name = "header_matches"];

      PathMatch path_match = 3 [json_name = "path_match"];

      message HeaderMatches {
        bool case_sensitive = 1 [json_name = "case_sensitive"];

        string name = 2;

        Match match = 3;

        message Match {
          string contains = 1;

          string exact = 2;

          string prefix = 3;
        }
      }

      message PathMatch {
        bool case_sensitive = 1 [json_name = "case_sensitive"];

        Match match = 2;

        message Match {
          string exact = 1;

          string prefix = 2;
        }
      }
    }
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// AwsVpclatticeResourceConfiguration version is 0
message AwsVpclatticeResourceConfiguration {
  bool allow_association_to_shareable_service_network = 1 [
    json_name = "allow_association_to_shareable_service_network"
  ];

  string arn = 2;

  string custom_domain_name = 3 [json_name = "custom_domain_name"];

  string domain_verification_arn = 4 [json_name = "domain_verification_arn"];

  string domain_verification_id = 5 [json_name = "domain_verification_id"];

  string domain_verification_status = 6 [json_name = "domain_verification_status"];

  string id = 7;

  string name = 8;

  repeated string port_ranges = 9 [json_name = "port_ranges"];

  string protocol = 10;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 11;

  string resource_configuration_group_id = 12 [json_name = "resource_configuration_group_id"];

  string resource_gateway_identifier = 13 [json_name = "resource_gateway_identifier"];

  map<string, string> tags = 14;

  map<string, string> tags_all = 15 [json_name = "tags_all"];

  string type = 16;

  repeated ResourceConfigurationDefinition resource_configuration_definition = 17 [json_name = "resource_configuration_definition"];

  repeated Timeouts timeouts = 18;

  google.protobuf.Value for_each = 19 [json_name = "for_each"];

  repeated string depends_on = 20 [json_name = "depends_on"];

  int32 count = 21;

  string provider = 22;

  terraform.v1.Lifecycle lifecycle = 23;

  message ResourceConfigurationDefinition {
    repeated ArnResource arn_resource = 1 [json_name = "arn_resource"];

    repeated DnsResource dns_resource = 2 [json_name = "dns_resource"];

    repeated IpResource ip_resource = 3 [json_name = "ip_resource"];

    message ArnResource {
      string arn = 1;
    }

    message DnsResource {
      string domain_name = 1 [json_name = "domain_name"];

      string ip_address_type = 2 [json_name = "ip_address_type"];
    }

    message IpResource {
      string ip_address = 1 [json_name = "ip_address"];
    }
  }

  message Timeouts {
    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours).
    string create = 1;

    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours). Setting a timeout for a Delete operation is only
    // applicable if changes are saved into state before the destroy operation occurs.
    string delete = 2;

    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours).
    string update = 3;
  }
}

// AwsVpclatticeResourceGateway version is 0
message AwsVpclatticeResourceGateway {
  string arn = 1;

  string id = 2;

  string ip_address_type = 3 [json_name = "ip_address_type"];

  int64 ipv4_addresses_per_eni = 4 [json_name = "ipv4_addresses_per_eni"];

  string name = 5;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 6;

  repeated string security_group_ids = 7 [json_name = "security_group_ids"];

  string status = 8;

  repeated string subnet_ids = 9 [json_name = "subnet_ids"];

  map<string, string> tags = 10;

  map<string, string> tags_all = 11 [json_name = "tags_all"];

  string vpc_id = 12 [json_name = "vpc_id"];

  repeated Timeouts timeouts = 13;

  google.protobuf.Value for_each = 14 [json_name = "for_each"];

  repeated string depends_on = 15 [json_name = "depends_on"];

  int32 count = 16;

  string provider = 17;

  terraform.v1.Lifecycle lifecycle = 18;

  message Timeouts {
    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours).
    string create = 1;

    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours). Setting a timeout for a Delete operation is only
    // applicable if changes are saved into state before the destroy operation occurs.
    string delete = 2;

    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours).
    string update = 3;
  }
}

// AwsVpclatticeResourcePolicy version is 0
message AwsVpclatticeResourcePolicy {
  string id = 1;

  string policy = 2;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 3;

  string resource_arn = 4 [json_name = "resource_arn"];

  google.protobuf.Value for_each = 5 [json_name = "for_each"];

  repeated string depends_on = 6 [json_name = "depends_on"];

  int32 count = 7;

  string provider = 8;

  terraform.v1.Lifecycle lifecycle = 9;
}

// AwsVpclatticeService version is 0
message AwsVpclatticeService {
  string arn = 1;

  string auth_type = 2 [json_name = "auth_type"];

  string certificate_arn = 3 [json_name = "certificate_arn"];

  string custom_domain_name = 4 [json_name = "custom_domain_name"];

  repeated DnsEntry dns_entry = 5 [json_name = "dns_entry"];

  string id = 6;

  string name = 7;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 8;

  string status = 9;

  map<string, string> tags = 10;

  map<string, string> tags_all = 11 [json_name = "tags_all"];

  repeated Timeouts timeouts = 12;

  google.protobuf.Value for_each = 13 [json_name = "for_each"];

  repeated string depends_on = 14 [json_name = "depends_on"];

  int32 count = 15;

  string provider = 16;

  terraform.v1.Lifecycle lifecycle = 17;

  message DnsEntry {
    // domain_name: string
    string domain_name = 1 [json_name = "domain_name"];

    // hosted_zone_id: string
    string hosted_zone_id = 2 [json_name = "hosted_zone_id"];
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// AwsVpclatticeServiceNetwork version is 0
message AwsVpclatticeServiceNetwork {
  string arn = 1;

  string auth_type = 2 [json_name = "auth_type"];

  string id = 3;

  string name = 4;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 5;

  map<string, string> tags = 6;

  map<string, string> tags_all = 7 [json_name = "tags_all"];

  google.protobuf.Value for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  int32 count = 10;

  string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;
}

// AwsVpclatticeServiceNetworkResourceAssociation version is 0
message AwsVpclatticeServiceNetworkResourceAssociation {
  string arn = 1;

  repeated DnsEntry dns_entry = 2 [json_name = "dns_entry"];

  string id = 3;

  bool private_dns_enabled = 4 [json_name = "private_dns_enabled"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 5;

  string resource_configuration_identifier = 6 [json_name = "resource_configuration_identifier"];

  string service_network_identifier = 7 [json_name = "service_network_identifier"];

  map<string, string> tags = 8;

  map<string, string> tags_all = 9 [json_name = "tags_all"];

  repeated Timeouts timeouts = 10;

  google.protobuf.Value for_each = 11 [json_name = "for_each"];

  repeated string depends_on = 12 [json_name = "depends_on"];

  int32 count = 13;

  string provider = 14;

  terraform.v1.Lifecycle lifecycle = 15;

  message DnsEntry {
    // domain_name: string
    string domain_name = 1 [json_name = "domain_name"];

    // hosted_zone_id: string
    string hosted_zone_id = 2 [json_name = "hosted_zone_id"];
  }

  message Timeouts {
    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours).
    string create = 1;

    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours). Setting a timeout for a Delete operation is only
    // applicable if changes are saved into state before the destroy operation occurs.
    string delete = 2;
  }
}

// AwsVpclatticeServiceNetworkServiceAssociation version is 0
message AwsVpclatticeServiceNetworkServiceAssociation {
  string arn = 1;

  string created_by = 2 [json_name = "created_by"];

  string custom_domain_name = 3 [json_name = "custom_domain_name"];

  repeated DnsEntry dns_entry = 4 [json_name = "dns_entry"];

  string id = 5;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 6;

  string service_identifier = 7 [json_name = "service_identifier"];

  string service_network_identifier = 8 [json_name = "service_network_identifier"];

  string status = 9;

  map<string, string> tags = 10;

  map<string, string> tags_all = 11 [json_name = "tags_all"];

  repeated Timeouts timeouts = 12;

  google.protobuf.Value for_each = 13 [json_name = "for_each"];

  repeated string depends_on = 14 [json_name = "depends_on"];

  int32 count = 15;

  string provider = 16;

  terraform.v1.Lifecycle lifecycle = 17;

  message DnsEntry {
    // domain_name: string
    string domain_name = 1 [json_name = "domain_name"];

    // hosted_zone_id: string
    string hosted_zone_id = 2 [json_name = "hosted_zone_id"];
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// AwsVpclatticeServiceNetworkVpcAssociation version is 0
message AwsVpclatticeServiceNetworkVpcAssociation {
  string arn = 1;

  string created_by = 2 [json_name = "created_by"];

  string id = 3;

  bool private_dns_enabled = 4 [json_name = "private_dns_enabled"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 5;

  repeated string security_group_ids = 6 [json_name = "security_group_ids"];

  string service_network_identifier = 7 [json_name = "service_network_identifier"];

  string status = 8;

  map<string, string> tags = 9;

  map<string, string> tags_all = 10 [json_name = "tags_all"];

  string vpc_identifier = 11 [json_name = "vpc_identifier"];

  DnsOptions dns_options = 12 [json_name = "dns_options"];

  repeated Timeouts timeouts = 13;

  google.protobuf.Value for_each = 14 [json_name = "for_each"];

  repeated string depends_on = 15 [json_name = "depends_on"];

  int32 count = 16;

  string provider = 17;

  terraform.v1.Lifecycle lifecycle = 18;

  message DnsOptions {
    string private_dns_preference = 1 [json_name = "private_dns_preference"];

    repeated string private_dns_specified_domains = 2 [json_name = "private_dns_specified_domains"];
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// AwsVpclatticeTargetGroup version is 0
message AwsVpclatticeTargetGroup {
  string arn = 1;

  string id = 2;

  string name = 3;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 4;

  string status = 5;

  map<string, string> tags = 6;

  map<string, string> tags_all = 7 [json_name = "tags_all"];

  string type = 8;

  Config config = 9;

  repeated Timeouts timeouts = 10;

  google.protobuf.Value for_each = 11 [json_name = "for_each"];

  repeated string depends_on = 12 [json_name = "depends_on"];

  int32 count = 13;

  string provider = 14;

  terraform.v1.Lifecycle lifecycle = 15;

  message Config {
    string ip_address_type = 1 [json_name = "ip_address_type"];

    string lambda_event_structure_version = 2 [json_name = "lambda_event_structure_version"];

    int64 port = 3;

    string protocol = 4;

    string protocol_version = 5 [json_name = "protocol_version"];

    string vpc_identifier = 6 [json_name = "vpc_identifier"];

    HealthCheck health_check = 7 [json_name = "health_check"];

    message HealthCheck {
      bool enabled = 1;

      int64 health_check_interval_seconds = 2 [json_name = "health_check_interval_seconds"];

      int64 health_check_timeout_seconds = 3 [json_name = "health_check_timeout_seconds"];

      int64 healthy_threshold_count = 4 [json_name = "healthy_threshold_count"];

      string path = 5;

      int64 port = 6;

      string protocol = 7;

      string protocol_version = 8 [json_name = "protocol_version"];

      int64 unhealthy_threshold_count = 9 [json_name = "unhealthy_threshold_count"];

      Matcher matcher = 10;

      message Matcher {
        string value = 1;
      }
    }
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}

// AwsVpclatticeTargetGroupAttachment version is 0
message AwsVpclatticeTargetGroupAttachment {
  string id = 1;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 2;

  string target_group_identifier = 3 [json_name = "target_group_identifier"];

  Target target = 4;

  repeated Timeouts timeouts = 5;

  google.protobuf.Value for_each = 6 [json_name = "for_each"];

  repeated string depends_on = 7 [json_name = "depends_on"];

  int32 count = 8;

  string provider = 9;

  terraform.v1.Lifecycle lifecycle = 10;

  message Target {
    string id = 1;

    int64 port = 2;
  }

  message Timeouts {
    string create = 1;

    string delete = 2;
  }
}
