syntax = "proto3";

// Provider: aws 6
package terraform.aws.resources.v6;

import "google/protobuf/struct.proto";

import "terraform/v1/meta.proto";

// AwsBatchComputeEnvironment version is 1
message AwsBatchComputeEnvironment {
  string arn = 1;

  string ecs_cluster_arn = 2 [json_name = "ecs_cluster_arn"];

  string id = 3;

  string name = 4;

  string name_prefix = 5 [json_name = "name_prefix"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 6;

  string service_role = 7 [json_name = "service_role"];

  string state = 8;

  string status = 9;

  string status_reason = 10 [json_name = "status_reason"];

  map<string, string> tags = 11;

  map<string, string> tags_all = 12 [json_name = "tags_all"];

  string type = 13;

  ComputeResources compute_resources = 14 [json_name = "compute_resources"];

  EksConfiguration eks_configuration = 15 [json_name = "eks_configuration"];

  UpdatePolicy update_policy = 16 [json_name = "update_policy"];

  google.protobuf.Value for_each = 17 [json_name = "for_each"];

  repeated string depends_on = 18 [json_name = "depends_on"];

  int32 count = 19;

  string provider = 20;

  terraform.v1.Lifecycle lifecycle = 21;

  message ComputeResources {
    string allocation_strategy = 1 [json_name = "allocation_strategy"];

    int64 bid_percentage = 2 [json_name = "bid_percentage"];

    int64 desired_vcpus = 3 [json_name = "desired_vcpus"];

    string ec2_key_pair = 4 [json_name = "ec2_key_pair"];

    string image_id = 5 [json_name = "image_id"];

    string instance_role = 6 [json_name = "instance_role"];

    repeated string instance_type = 7 [json_name = "instance_type"];

    int64 max_vcpus = 8 [json_name = "max_vcpus"];

    int64 min_vcpus = 9 [json_name = "min_vcpus"];

    string placement_group = 10 [json_name = "placement_group"];

    repeated string security_group_ids = 11 [json_name = "security_group_ids"];

    string spot_iam_fleet_role = 12 [json_name = "spot_iam_fleet_role"];

    repeated string subnets = 13;

    map<string, string> tags = 14;

    string type = 15;

    repeated Ec2Configuration ec2_configuration = 16 [json_name = "ec2_configuration"];

    LaunchTemplate launch_template = 17 [json_name = "launch_template"];

    message Ec2Configuration {
      string image_id_override = 1 [json_name = "image_id_override"];

      string image_kubernetes_version = 2 [json_name = "image_kubernetes_version"];

      string image_type = 3 [json_name = "image_type"];
    }

    message LaunchTemplate {
      string launch_template_id = 1 [json_name = "launch_template_id"];

      string launch_template_name = 2 [json_name = "launch_template_name"];

      string version = 3;
    }
  }

  message EksConfiguration {
    string eks_cluster_arn = 1 [json_name = "eks_cluster_arn"];

    string kubernetes_namespace = 2 [json_name = "kubernetes_namespace"];
  }

  message UpdatePolicy {
    int64 job_execution_timeout_minutes = 1 [json_name = "job_execution_timeout_minutes"];

    bool terminate_jobs_on_update = 2 [json_name = "terminate_jobs_on_update"];
  }
}

// AwsBatchJobDefinition version is 0
message AwsBatchJobDefinition {
  string arn = 1;

  string arn_prefix = 2 [json_name = "arn_prefix"];

  string container_properties = 3 [json_name = "container_properties"];

  bool deregister_on_new_revision = 4 [json_name = "deregister_on_new_revision"];

  string ecs_properties = 5 [json_name = "ecs_properties"];

  string id = 6;

  string name = 7;

  string node_properties = 8 [json_name = "node_properties"];

  map<string, string> parameters = 9;

  repeated string platform_capabilities = 10 [json_name = "platform_capabilities"];

  bool propagate_tags = 11 [json_name = "propagate_tags"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 12;

  int64 revision = 13;

  int64 scheduling_priority = 14 [json_name = "scheduling_priority"];

  map<string, string> tags = 15;

  map<string, string> tags_all = 16 [json_name = "tags_all"];

  string type = 17;

  EksProperties eks_properties = 18 [json_name = "eks_properties"];

  RetryStrategy retry_strategy = 19 [json_name = "retry_strategy"];

  Timeout timeout = 20;

  google.protobuf.Value for_each = 21 [json_name = "for_each"];

  repeated string depends_on = 22 [json_name = "depends_on"];

  int32 count = 23;

  string provider = 24;

  terraform.v1.Lifecycle lifecycle = 25;

  message EksProperties {
    PodProperties pod_properties = 1 [json_name = "pod_properties"];

    message PodProperties {
      string dns_policy = 1 [json_name = "dns_policy"];

      bool host_network = 2 [json_name = "host_network"];

      string service_account_name = 3 [json_name = "service_account_name"];

      bool share_process_namespace = 4 [json_name = "share_process_namespace"];

      repeated Containers containers = 5;

      repeated ImagePullSecret image_pull_secret = 6 [json_name = "image_pull_secret"];

      repeated InitContainers init_containers = 7 [json_name = "init_containers"];

      Metadata metadata = 8;

      repeated Volumes volumes = 9;

      message Containers {
        repeated string args = 1;

        repeated string command = 2;

        string image = 3;

        string image_pull_policy = 4 [json_name = "image_pull_policy"];

        string name = 5;

        repeated Env env = 6;

        Resources resources = 7;

        SecurityContext security_context = 8 [json_name = "security_context"];

        repeated VolumeMounts volume_mounts = 9 [json_name = "volume_mounts"];

        message Env {
          string name = 1;

          string value = 2;
        }

        message Resources {
          map<string, string> limits = 1;

          map<string, string> requests = 2;
        }

        message SecurityContext {
          bool allow_privilege_escalation = 1 [json_name = "allow_privilege_escalation"];

          bool privileged = 2;

          bool read_only_root_file_system = 3 [json_name = "read_only_root_file_system"];

          int64 run_as_group = 4 [json_name = "run_as_group"];

          bool run_as_non_root = 5 [json_name = "run_as_non_root"];

          int64 run_as_user = 6 [json_name = "run_as_user"];
        }

        message VolumeMounts {
          string mount_path = 1 [json_name = "mount_path"];

          string name = 2;

          bool read_only = 3 [json_name = "read_only"];
        }
      }

      message ImagePullSecret {
        string name = 1;
      }

      message InitContainers {
        repeated string args = 1;

        repeated string command = 2;

        string image = 3;

        string image_pull_policy = 4 [json_name = "image_pull_policy"];

        string name = 5;

        repeated Env env = 6;

        Resources resources = 7;

        SecurityContext security_context = 8 [json_name = "security_context"];

        repeated VolumeMounts volume_mounts = 9 [json_name = "volume_mounts"];

        message Env {
          string name = 1;

          string value = 2;
        }

        message Resources {
          map<string, string> limits = 1;

          map<string, string> requests = 2;
        }

        message SecurityContext {
          bool allow_privilege_escalation = 1 [json_name = "allow_privilege_escalation"];

          bool privileged = 2;

          bool read_only_root_file_system = 3 [json_name = "read_only_root_file_system"];

          int64 run_as_group = 4 [json_name = "run_as_group"];

          bool run_as_non_root = 5 [json_name = "run_as_non_root"];

          int64 run_as_user = 6 [json_name = "run_as_user"];
        }

        message VolumeMounts {
          string mount_path = 1 [json_name = "mount_path"];

          string name = 2;

          bool read_only = 3 [json_name = "read_only"];
        }
      }

      message Metadata {
        map<string, string> labels = 1;
      }

      message Volumes {
        string name = 1;

        EmptyDir empty_dir = 2 [json_name = "empty_dir"];

        HostPath host_path = 3 [json_name = "host_path"];

        Secret secret = 4;

        message EmptyDir {
          string medium = 1;

          string size_limit = 2 [json_name = "size_limit"];
        }

        message HostPath {
          string path = 1;
        }

        message Secret {
          bool optional = 1;

          string secret_name = 2 [json_name = "secret_name"];
        }
      }
    }
  }

  message RetryStrategy {
    int64 attempts = 1;

    repeated EvaluateOnExit evaluate_on_exit = 2 [json_name = "evaluate_on_exit"];

    message EvaluateOnExit {
      string action = 1;

      string on_exit_code = 2 [json_name = "on_exit_code"];

      string on_reason = 3 [json_name = "on_reason"];

      string on_status_reason = 4 [json_name = "on_status_reason"];
    }
  }

  message Timeout {
    int64 attempt_duration_seconds = 1 [json_name = "attempt_duration_seconds"];
  }
}

// AwsBatchJobQueue version is 2
message AwsBatchJobQueue {
  string arn = 1;

  string id = 2;

  string name = 3;

  int64 priority = 4;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 5;

  string scheduling_policy_arn = 6 [json_name = "scheduling_policy_arn"];

  string state = 7;

  map<string, string> tags = 8;

  map<string, string> tags_all = 9 [json_name = "tags_all"];

  repeated ComputeEnvironmentOrder compute_environment_order = 10 [json_name = "compute_environment_order"];

  repeated JobStateTimeLimitAction job_state_time_limit_action = 11 [json_name = "job_state_time_limit_action"];

  repeated Timeouts timeouts = 12;

  google.protobuf.Value for_each = 13 [json_name = "for_each"];

  repeated string depends_on = 14 [json_name = "depends_on"];

  int32 count = 15;

  string provider = 16;

  terraform.v1.Lifecycle lifecycle = 17;

  message ComputeEnvironmentOrder {
    string compute_environment = 1 [json_name = "compute_environment"];

    int64 order = 2;
  }

  message JobStateTimeLimitAction {
    string action = 1;

    int64 max_time_seconds = 2 [json_name = "max_time_seconds"];

    string reason = 3;

    string state = 4;
  }

  message Timeouts {
    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours).
    string create = 1;

    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours). Setting a timeout for a Delete operation is only
    // applicable if changes are saved into state before the destroy operation occurs.
    string delete = 2;

    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours).
    string update = 3;
  }
}

// AwsBatchSchedulingPolicy version is 0
message AwsBatchSchedulingPolicy {
  string arn = 1;

  string id = 2;

  string name = 3;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 4;

  map<string, string> tags = 5;

  map<string, string> tags_all = 6 [json_name = "tags_all"];

  FairSharePolicy fair_share_policy = 7 [json_name = "fair_share_policy"];

  google.protobuf.Value for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  int32 count = 10;

  string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;

  message FairSharePolicy {
    int64 compute_reservation = 1 [json_name = "compute_reservation"];

    int64 share_decay_seconds = 2 [json_name = "share_decay_seconds"];

    repeated ShareDistribution share_distribution = 3 [json_name = "share_distribution"];

    message ShareDistribution {
      string share_identifier = 1 [json_name = "share_identifier"];

      int64 weight_factor = 2 [json_name = "weight_factor"];
    }
  }
}
