syntax = "proto3";

// Provider: aws 6
package terraform.aws.resources.v6;

import "google/protobuf/struct.proto";

import "terraform/v1/meta.proto";

// AwsBedrockCustomModel version is 0
message AwsBedrockCustomModel {
  string base_model_identifier = 1 [json_name = "base_model_identifier"];

  string custom_model_arn = 2 [json_name = "custom_model_arn"];

  string custom_model_kms_key_id = 3 [json_name = "custom_model_kms_key_id"];

  string custom_model_name = 4 [json_name = "custom_model_name"];

  string customization_type = 5 [json_name = "customization_type"];

  map<string, string> hyperparameters = 6;

  string id = 7;

  string job_arn = 8 [json_name = "job_arn"];

  string job_name = 9 [json_name = "job_name"];

  string job_status = 10 [json_name = "job_status"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 11;

  string role_arn = 12 [json_name = "role_arn"];

  map<string, string> tags = 13;

  map<string, string> tags_all = 14 [json_name = "tags_all"];

  repeated TrainingMetrics training_metrics = 15 [json_name = "training_metrics"];

  repeated ValidationMetrics validation_metrics = 16 [json_name = "validation_metrics"];

  repeated OutputDataConfig output_data_config = 17 [json_name = "output_data_config"];

  repeated Timeouts timeouts = 18;

  repeated TrainingDataConfig training_data_config = 19 [json_name = "training_data_config"];

  repeated ValidationDataConfig validation_data_config = 20 [json_name = "validation_data_config"];

  repeated VpcConfig vpc_config = 21 [json_name = "vpc_config"];

  google.protobuf.Value for_each = 22 [json_name = "for_each"];

  repeated string depends_on = 23 [json_name = "depends_on"];

  int32 count = 24;

  string provider = 25;

  terraform.v1.Lifecycle lifecycle = 26;

  message TrainingMetrics {
    // training_loss: number
    int64 training_loss = 1 [json_name = "training_loss"];
  }

  message ValidationMetrics {
    // validation_loss: number
    int64 validation_loss = 1 [json_name = "validation_loss"];
  }

  message OutputDataConfig {
    string s3_uri = 1 [json_name = "s3_uri"];
  }

  message Timeouts {
    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours).
    string create = 1;

    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours). Setting a timeout for a Delete operation is only
    // applicable if changes are saved into state before the destroy operation occurs.
    string delete = 2;
  }

  message TrainingDataConfig {
    string s3_uri = 1 [json_name = "s3_uri"];
  }

  message ValidationDataConfig {
    repeated Validator validator = 1;

    message Validator {
      string s3_uri = 1 [json_name = "s3_uri"];
    }
  }

  message VpcConfig {
    repeated string security_group_ids = 1 [json_name = "security_group_ids"];

    repeated string subnet_ids = 2 [json_name = "subnet_ids"];
  }
}

// AwsBedrockGuardrail version is 0
message AwsBedrockGuardrail {
  string blocked_input_messaging = 1 [json_name = "blocked_input_messaging"];

  string blocked_outputs_messaging = 2 [json_name = "blocked_outputs_messaging"];

  string created_at = 3 [json_name = "created_at"];

  string description = 4;

  string guardrail_arn = 5 [json_name = "guardrail_arn"];

  string guardrail_id = 6 [json_name = "guardrail_id"];

  string kms_key_arn = 7 [json_name = "kms_key_arn"];

  string name = 8;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 9;

  string status = 10;

  map<string, string> tags = 11;

  map<string, string> tags_all = 12 [json_name = "tags_all"];

  string version = 13;

  repeated ContentPolicyConfig content_policy_config = 14 [json_name = "content_policy_config"];

  repeated ContextualGroundingPolicyConfig contextual_grounding_policy_config = 15 [json_name = "contextual_grounding_policy_config"];

  repeated CrossRegionConfig cross_region_config = 16 [json_name = "cross_region_config"];

  repeated SensitiveInformationPolicyConfig sensitive_information_policy_config = 17 [json_name = "sensitive_information_policy_config"];

  repeated Timeouts timeouts = 18;

  repeated TopicPolicyConfig topic_policy_config = 19 [json_name = "topic_policy_config"];

  repeated WordPolicyConfig word_policy_config = 20 [json_name = "word_policy_config"];

  google.protobuf.Value for_each = 21 [json_name = "for_each"];

  repeated string depends_on = 22 [json_name = "depends_on"];

  int32 count = 23;

  string provider = 24;

  terraform.v1.Lifecycle lifecycle = 25;

  message ContentPolicyConfig {
    repeated TierConfig tier_config = 1 [json_name = "tier_config"];

    repeated FiltersConfig filters_config = 2 [json_name = "filters_config"];

    message TierConfig {
      // tier_name: string
      string tier_name = 1 [json_name = "tier_name"];
    }

    message FiltersConfig {
      string input_action = 1 [json_name = "input_action"];

      bool input_enabled = 2 [json_name = "input_enabled"];

      repeated string input_modalities = 3 [json_name = "input_modalities"];

      string input_strength = 4 [json_name = "input_strength"];

      string output_action = 5 [json_name = "output_action"];

      bool output_enabled = 6 [json_name = "output_enabled"];

      repeated string output_modalities = 7 [json_name = "output_modalities"];

      string output_strength = 8 [json_name = "output_strength"];

      string type = 9;
    }
  }

  message ContextualGroundingPolicyConfig {
    repeated FiltersConfig filters_config = 1 [json_name = "filters_config"];

    message FiltersConfig {
      int64 threshold = 1;

      string type = 2;
    }
  }

  message CrossRegionConfig {
    string guardrail_profile_identifier = 1 [json_name = "guardrail_profile_identifier"];
  }

  message SensitiveInformationPolicyConfig {
    repeated PiiEntitiesConfig pii_entities_config = 1 [json_name = "pii_entities_config"];

    repeated RegexesConfig regexes_config = 2 [json_name = "regexes_config"];

    message PiiEntitiesConfig {
      string action = 1;

      string input_action = 2 [json_name = "input_action"];

      bool input_enabled = 3 [json_name = "input_enabled"];

      string output_action = 4 [json_name = "output_action"];

      bool output_enabled = 5 [json_name = "output_enabled"];

      string type = 6;
    }

    message RegexesConfig {
      string action = 1;

      string description = 2;

      string input_action = 3 [json_name = "input_action"];

      bool input_enabled = 4 [json_name = "input_enabled"];

      string name = 5;

      string output_action = 6 [json_name = "output_action"];

      bool output_enabled = 7 [json_name = "output_enabled"];

      string pattern = 8;
    }
  }

  message Timeouts {
    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours).
    string create = 1;

    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours). Setting a timeout for a Delete operation is only
    // applicable if changes are saved into state before the destroy operation occurs.
    string delete = 2;

    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours).
    string update = 3;
  }

  message TopicPolicyConfig {
    repeated TierConfig tier_config = 1 [json_name = "tier_config"];

    repeated TopicsConfig topics_config = 2 [json_name = "topics_config"];

    message TierConfig {
      // tier_name: string
      string tier_name = 1 [json_name = "tier_name"];
    }

    message TopicsConfig {
      string definition = 1;

      repeated string examples = 2;

      string name = 3;

      string type = 4;
    }
  }

  message WordPolicyConfig {
    repeated ManagedWordListsConfig managed_word_lists_config = 1 [json_name = "managed_word_lists_config"];

    repeated WordsConfig words_config = 2 [json_name = "words_config"];

    message ManagedWordListsConfig {
      string input_action = 1 [json_name = "input_action"];

      bool input_enabled = 2 [json_name = "input_enabled"];

      string output_action = 3 [json_name = "output_action"];

      bool output_enabled = 4 [json_name = "output_enabled"];

      string type = 5;
    }

    message WordsConfig {
      string input_action = 1 [json_name = "input_action"];

      bool input_enabled = 2 [json_name = "input_enabled"];

      string output_action = 3 [json_name = "output_action"];

      bool output_enabled = 4 [json_name = "output_enabled"];

      string text = 5;
    }
  }
}

// AwsBedrockGuardrailVersion version is 0
message AwsBedrockGuardrailVersion {
  string description = 1;

  string guardrail_arn = 2 [json_name = "guardrail_arn"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 3;

  bool skip_destroy = 4 [json_name = "skip_destroy"];

  string version = 5;

  repeated Timeouts timeouts = 6;

  google.protobuf.Value for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  int32 count = 9;

  string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;

  message Timeouts {
    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours).
    string create = 1;

    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours). Setting a timeout for a Delete operation is only
    // applicable if changes are saved into state before the destroy operation occurs.
    string delete = 2;
  }
}

// AwsBedrockInferenceProfile version is 0
message AwsBedrockInferenceProfile {
  string arn = 1;

  string created_at = 2 [json_name = "created_at"];

  string description = 3;

  string id = 4;

  repeated Models models = 5;

  string name = 6;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 7;

  string status = 8;

  map<string, string> tags = 9;

  map<string, string> tags_all = 10 [json_name = "tags_all"];

  string type = 11;

  string updated_at = 12 [json_name = "updated_at"];

  repeated ModelSource model_source = 13 [json_name = "model_source"];

  repeated Timeouts timeouts = 14;

  google.protobuf.Value for_each = 15 [json_name = "for_each"];

  repeated string depends_on = 16 [json_name = "depends_on"];

  int32 count = 17;

  string provider = 18;

  terraform.v1.Lifecycle lifecycle = 19;

  message Models {
    // model_arn: string
    string model_arn = 1 [json_name = "model_arn"];
  }

  message ModelSource {
    string copy_from = 1 [json_name = "copy_from"];
  }

  message Timeouts {
    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours).
    string create = 1;

    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours). Setting a timeout for a Delete operation is only
    // applicable if changes are saved into state before the destroy operation occurs.
    string delete = 2;

    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours).
    string update = 3;
  }
}

// AwsBedrockModelInvocationLoggingConfiguration version is 1
message AwsBedrockModelInvocationLoggingConfiguration {
  string id = 1;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 2;

  repeated LoggingConfig logging_config = 3 [json_name = "logging_config"];

  google.protobuf.Value for_each = 4 [json_name = "for_each"];

  repeated string depends_on = 5 [json_name = "depends_on"];

  int32 count = 6;

  string provider = 7;

  terraform.v1.Lifecycle lifecycle = 8;

  message LoggingConfig {
    bool embedding_data_delivery_enabled = 1 [json_name = "embedding_data_delivery_enabled"];

    bool image_data_delivery_enabled = 2 [json_name = "image_data_delivery_enabled"];

    bool text_data_delivery_enabled = 3 [json_name = "text_data_delivery_enabled"];

    bool video_data_delivery_enabled = 4 [json_name = "video_data_delivery_enabled"];

    repeated CloudwatchConfig cloudwatch_config = 5 [json_name = "cloudwatch_config"];

    repeated S3Config s3_config = 6 [json_name = "s3_config"];

    message CloudwatchConfig {
      string log_group_name = 1 [json_name = "log_group_name"];

      string role_arn = 2 [json_name = "role_arn"];

      repeated LargeDataDeliveryS3Config large_data_delivery_s3_config = 3 [json_name = "large_data_delivery_s3_config"];

      message LargeDataDeliveryS3Config {
        string bucket_name = 1 [json_name = "bucket_name"];

        string key_prefix = 2 [json_name = "key_prefix"];
      }
    }

    message S3Config {
      string bucket_name = 1 [json_name = "bucket_name"];

      string key_prefix = 2 [json_name = "key_prefix"];
    }
  }
}

// AwsBedrockProvisionedModelThroughput version is 0
message AwsBedrockProvisionedModelThroughput {
  string commitment_duration = 1 [json_name = "commitment_duration"];

  string id = 2;

  string model_arn = 3 [json_name = "model_arn"];

  int64 model_units = 4 [json_name = "model_units"];

  string provisioned_model_arn = 5 [json_name = "provisioned_model_arn"];

  string provisioned_model_name = 6 [json_name = "provisioned_model_name"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 7;

  map<string, string> tags = 8;

  map<string, string> tags_all = 9 [json_name = "tags_all"];

  repeated Timeouts timeouts = 10;

  google.protobuf.Value for_each = 11 [json_name = "for_each"];

  repeated string depends_on = 12 [json_name = "depends_on"];

  int32 count = 13;

  string provider = 14;

  terraform.v1.Lifecycle lifecycle = 15;

  message Timeouts {
    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours).
    string create = 1;
  }
}
