syntax = "proto3";

// Provider: aws 6
package terraform.aws.resources.v6;

import "google/protobuf/struct.proto";

import "terraform/v1/meta.proto";

// AwsAcmpcaCertificate version is 0
message AwsAcmpcaCertificate {
  string api_passthrough = 1 [json_name = "api_passthrough"];

  string arn = 2;

  string certificate = 3;

  string certificate_authority_arn = 4 [json_name = "certificate_authority_arn"];

  string certificate_chain = 5 [json_name = "certificate_chain"];

  string certificate_signing_request = 6 [json_name = "certificate_signing_request"];

  string id = 7;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 8;

  string signing_algorithm = 9 [json_name = "signing_algorithm"];

  string template_arn = 10 [json_name = "template_arn"];

  Validity validity = 11;

  google.protobuf.Value for_each = 12 [json_name = "for_each"];

  repeated string depends_on = 13 [json_name = "depends_on"];

  int32 count = 14;

  string provider = 15;

  terraform.v1.Lifecycle lifecycle = 16;

  message Validity {
    string type = 1;

    string value = 2;
  }
}

// AwsAcmpcaCertificateAuthority version is 1
message AwsAcmpcaCertificateAuthority {
  string arn = 1;

  string certificate = 2;

  string certificate_chain = 3 [json_name = "certificate_chain"];

  string certificate_signing_request = 4 [json_name = "certificate_signing_request"];

  bool enabled = 5;

  string id = 6;

  string key_storage_security_standard = 7 [json_name = "key_storage_security_standard"];

  string not_after = 8 [json_name = "not_after"];

  string not_before = 9 [json_name = "not_before"];

  int64 permanent_deletion_time_in_days = 10 [json_name = "permanent_deletion_time_in_days"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 11;

  string serial = 12;

  map<string, string> tags = 13;

  map<string, string> tags_all = 14 [json_name = "tags_all"];

  string type = 15;

  string usage_mode = 16 [json_name = "usage_mode"];

  CertificateAuthorityConfiguration certificate_authority_configuration = 17 [json_name = "certificate_authority_configuration"];

  RevocationConfiguration revocation_configuration = 18 [json_name = "revocation_configuration"];

  repeated Timeouts timeouts = 19;

  google.protobuf.Value for_each = 20 [json_name = "for_each"];

  repeated string depends_on = 21 [json_name = "depends_on"];

  int32 count = 22;

  string provider = 23;

  terraform.v1.Lifecycle lifecycle = 24;

  message CertificateAuthorityConfiguration {
    string key_algorithm = 1 [json_name = "key_algorithm"];

    string signing_algorithm = 2 [json_name = "signing_algorithm"];

    Subject subject = 3;

    message Subject {
      string common_name = 1 [json_name = "common_name"];

      string country = 2;

      string distinguished_name_qualifier = 3 [json_name = "distinguished_name_qualifier"];

      string generation_qualifier = 4 [json_name = "generation_qualifier"];

      string given_name = 5 [json_name = "given_name"];

      string initials = 6;

      string locality = 7;

      string organization = 8;

      string organizational_unit = 9 [json_name = "organizational_unit"];

      string pseudonym = 10;

      string state = 11;

      string surname = 12;

      string title = 13;
    }
  }

  message RevocationConfiguration {
    CrlConfiguration crl_configuration = 1 [json_name = "crl_configuration"];

    OcspConfiguration ocsp_configuration = 2 [json_name = "ocsp_configuration"];

    message CrlConfiguration {
      string custom_cname = 1 [json_name = "custom_cname"];

      string custom_path = 2 [json_name = "custom_path"];

      bool enabled = 3;

      int64 expiration_in_days = 4 [json_name = "expiration_in_days"];

      string s3_bucket_name = 5 [json_name = "s3_bucket_name"];

      string s3_object_acl = 6 [json_name = "s3_object_acl"];
    }

    message OcspConfiguration {
      bool enabled = 1;

      string ocsp_custom_cname = 2 [json_name = "ocsp_custom_cname"];
    }
  }

  message Timeouts {
    string create = 1;
  }
}

// AwsAcmpcaCertificateAuthorityCertificate version is 0
message AwsAcmpcaCertificateAuthorityCertificate {
  string certificate = 1;

  string certificate_authority_arn = 2 [json_name = "certificate_authority_arn"];

  string certificate_chain = 3 [json_name = "certificate_chain"];

  string id = 4;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 5;

  google.protobuf.Value for_each = 6 [json_name = "for_each"];

  repeated string depends_on = 7 [json_name = "depends_on"];

  int32 count = 8;

  string provider = 9;

  terraform.v1.Lifecycle lifecycle = 10;
}

// AwsAcmpcaPermission version is 0
message AwsAcmpcaPermission {
  repeated string actions = 1;

  string certificate_authority_arn = 2 [json_name = "certificate_authority_arn"];

  string id = 3;

  string policy = 4;

  string principal = 5;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 6;

  string source_account = 7 [json_name = "source_account"];

  google.protobuf.Value for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  int32 count = 10;

  string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;
}

// AwsAcmpcaPolicy version is 0
message AwsAcmpcaPolicy {
  string id = 1;

  string policy = 2;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 3;

  string resource_arn = 4 [json_name = "resource_arn"];

  google.protobuf.Value for_each = 5 [json_name = "for_each"];

  repeated string depends_on = 6 [json_name = "depends_on"];

  int32 count = 7;

  string provider = 8;

  terraform.v1.Lifecycle lifecycle = 9;
}
