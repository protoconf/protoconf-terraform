syntax = "proto3";

// Provider: aws 6
package terraform.aws.resources.v6;

import "google/protobuf/struct.proto";

import "terraform/v1/meta.proto";

// AwsTimestreamqueryScheduledQuery version is 0
message AwsTimestreamqueryScheduledQuery {
  string arn = 1;

  string creation_time = 2 [json_name = "creation_time"];

  string execution_role_arn = 3 [json_name = "execution_role_arn"];

  string kms_key_id = 4 [json_name = "kms_key_id"];

  string name = 5;

  string next_invocation_time = 6 [json_name = "next_invocation_time"];

  string previous_invocation_time = 7 [json_name = "previous_invocation_time"];

  string query_string = 8 [json_name = "query_string"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 9;

  string state = 10;

  map<string, string> tags = 11;

  map<string, string> tags_all = 12 [json_name = "tags_all"];

  repeated ErrorReportConfiguration error_report_configuration = 13 [json_name = "error_report_configuration"];

  repeated LastRunSummary last_run_summary = 14 [json_name = "last_run_summary"];

  repeated NotificationConfiguration notification_configuration = 15 [json_name = "notification_configuration"];

  repeated RecentlyFailedRuns recently_failed_runs = 16 [json_name = "recently_failed_runs"];

  repeated ScheduleConfiguration schedule_configuration = 17 [json_name = "schedule_configuration"];

  repeated TargetConfiguration target_configuration = 18 [json_name = "target_configuration"];

  repeated Timeouts timeouts = 19;

  google.protobuf.Value for_each = 20 [json_name = "for_each"];

  repeated string depends_on = 21 [json_name = "depends_on"];

  int32 count = 22;

  string provider = 23;

  terraform.v1.Lifecycle lifecycle = 24;

  message ErrorReportConfiguration {
    repeated S3Configuration s3_configuration = 1 [json_name = "s3_configuration"];

    message S3Configuration {
      string bucket_name = 1 [json_name = "bucket_name"];

      string encryption_option = 2 [json_name = "encryption_option"];

      string object_key_prefix = 3 [json_name = "object_key_prefix"];
    }
  }

  message LastRunSummary {
    string failure_reason = 1 [json_name = "failure_reason"];

    string invocation_time = 2 [json_name = "invocation_time"];

    string run_status = 3 [json_name = "run_status"];

    string trigger_time = 4 [json_name = "trigger_time"];

    repeated ErrorReportLocation error_report_location = 5 [json_name = "error_report_location"];

    repeated ExecutionStats execution_stats = 6 [json_name = "execution_stats"];

    repeated QueryInsightsResponse query_insights_response = 7 [json_name = "query_insights_response"];

    message ErrorReportLocation {
      repeated S3ReportLocation s3_report_location = 1 [json_name = "s3_report_location"];

      message S3ReportLocation {
        string bucket_name = 1 [json_name = "bucket_name"];

        string object_key = 2 [json_name = "object_key"];
      }
    }

    message ExecutionStats {
      int64 bytes_metered = 1 [json_name = "bytes_metered"];

      int64 cumulative_bytes_scanned = 2 [json_name = "cumulative_bytes_scanned"];

      int64 data_writes = 3 [json_name = "data_writes"];

      int64 execution_time_in_millis = 4 [json_name = "execution_time_in_millis"];

      int64 query_result_rows = 5 [json_name = "query_result_rows"];

      int64 records_ingested = 6 [json_name = "records_ingested"];
    }

    message QueryInsightsResponse {
      int64 output_bytes = 1 [json_name = "output_bytes"];

      int64 output_rows = 2 [json_name = "output_rows"];

      int64 query_table_count = 3 [json_name = "query_table_count"];

      repeated QuerySpatialCoverage query_spatial_coverage = 4 [json_name = "query_spatial_coverage"];

      repeated QueryTemporalRange query_temporal_range = 5 [json_name = "query_temporal_range"];

      message QuerySpatialCoverage {
        repeated Max max = 1;

        message Max {
          repeated string partition_key = 1 [json_name = "partition_key"];

          string table_arn = 2 [json_name = "table_arn"];

          int64 value = 3;
        }
      }

      message QueryTemporalRange {
        repeated Max max = 1;

        message Max {
          string table_arn = 1 [json_name = "table_arn"];

          int64 value = 2;
        }
      }
    }
  }

  message NotificationConfiguration {
    repeated SnsConfiguration sns_configuration = 1 [json_name = "sns_configuration"];

    message SnsConfiguration {
      string topic_arn = 1 [json_name = "topic_arn"];
    }
  }

  message RecentlyFailedRuns {
    string failure_reason = 1 [json_name = "failure_reason"];

    string invocation_time = 2 [json_name = "invocation_time"];

    string run_status = 3 [json_name = "run_status"];

    string trigger_time = 4 [json_name = "trigger_time"];

    repeated ErrorReportLocation error_report_location = 5 [json_name = "error_report_location"];

    repeated ExecutionStats execution_stats = 6 [json_name = "execution_stats"];

    repeated QueryInsightsResponse query_insights_response = 7 [json_name = "query_insights_response"];

    message ErrorReportLocation {
      repeated S3ReportLocation s3_report_location = 1 [json_name = "s3_report_location"];

      message S3ReportLocation {
        string bucket_name = 1 [json_name = "bucket_name"];

        string object_key = 2 [json_name = "object_key"];
      }
    }

    message ExecutionStats {
      int64 bytes_metered = 1 [json_name = "bytes_metered"];

      int64 cumulative_bytes_scanned = 2 [json_name = "cumulative_bytes_scanned"];

      int64 data_writes = 3 [json_name = "data_writes"];

      int64 execution_time_in_millis = 4 [json_name = "execution_time_in_millis"];

      int64 query_result_rows = 5 [json_name = "query_result_rows"];

      int64 records_ingested = 6 [json_name = "records_ingested"];
    }

    message QueryInsightsResponse {
      int64 output_bytes = 1 [json_name = "output_bytes"];

      int64 output_rows = 2 [json_name = "output_rows"];

      int64 query_table_count = 3 [json_name = "query_table_count"];

      repeated QuerySpatialCoverage query_spatial_coverage = 4 [json_name = "query_spatial_coverage"];

      repeated QueryTemporalRange query_temporal_range = 5 [json_name = "query_temporal_range"];

      message QuerySpatialCoverage {
        repeated Max max = 1;

        message Max {
          repeated string partition_key = 1 [json_name = "partition_key"];

          string table_arn = 2 [json_name = "table_arn"];

          int64 value = 3;
        }
      }

      message QueryTemporalRange {
        repeated Max max = 1;

        message Max {
          string table_arn = 1 [json_name = "table_arn"];

          int64 value = 2;
        }
      }
    }
  }

  message ScheduleConfiguration {
    string schedule_expression = 1 [json_name = "schedule_expression"];
  }

  message TargetConfiguration {
    repeated TimestreamConfiguration timestream_configuration = 1 [json_name = "timestream_configuration"];

    message TimestreamConfiguration {
      string database_name = 1 [json_name = "database_name"];

      string measure_name_column = 2 [json_name = "measure_name_column"];

      string table_name = 3 [json_name = "table_name"];

      string time_column = 4 [json_name = "time_column"];

      repeated DimensionMapping dimension_mapping = 5 [json_name = "dimension_mapping"];

      repeated MixedMeasureMapping mixed_measure_mapping = 6 [json_name = "mixed_measure_mapping"];

      repeated MultiMeasureMappings multi_measure_mappings = 7 [json_name = "multi_measure_mappings"];

      message DimensionMapping {
        string dimension_value_type = 1 [json_name = "dimension_value_type"];

        string name = 2;
      }

      message MixedMeasureMapping {
        string measure_name = 1 [json_name = "measure_name"];

        string measure_value_type = 2 [json_name = "measure_value_type"];

        string source_column = 3 [json_name = "source_column"];

        string target_measure_name = 4 [json_name = "target_measure_name"];

        repeated MultiMeasureAttributeMapping multi_measure_attribute_mapping = 5 [json_name = "multi_measure_attribute_mapping"];

        message MultiMeasureAttributeMapping {
          string measure_value_type = 1 [json_name = "measure_value_type"];

          string source_column = 2 [json_name = "source_column"];

          string target_multi_measure_attribute_name = 3 [json_name = "target_multi_measure_attribute_name"];
        }
      }

      message MultiMeasureMappings {
        string target_multi_measure_name = 1 [json_name = "target_multi_measure_name"];

        repeated MultiMeasureAttributeMapping multi_measure_attribute_mapping = 2 [json_name = "multi_measure_attribute_mapping"];

        message MultiMeasureAttributeMapping {
          string measure_value_type = 1 [json_name = "measure_value_type"];

          string source_column = 2 [json_name = "source_column"];

          string target_multi_measure_attribute_name = 3 [json_name = "target_multi_measure_attribute_name"];
        }
      }
    }
  }

  message Timeouts {
    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours).
    string create = 1;

    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours). Setting a timeout for a Delete operation is only
    // applicable if changes are saved into state before the destroy operation occurs.
    string delete = 2;

    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours).
    string update = 3;
  }
}
