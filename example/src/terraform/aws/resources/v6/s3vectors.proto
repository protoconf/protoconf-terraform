syntax = "proto3";

// Provider: aws 6
package terraform.aws.resources.v6;

import "google/protobuf/struct.proto";

import "terraform/v1/meta.proto";

// AwsS3vectorsIndex version is 0
message AwsS3vectorsIndex {
  string creation_time = 1 [json_name = "creation_time"];

  string data_type = 2 [json_name = "data_type"];

  int64 dimension = 3;

  string distance_metric = 4 [json_name = "distance_metric"];

  repeated EncryptionConfiguration encryption_configuration = 5 [json_name = "encryption_configuration"];

  string index_arn = 6 [json_name = "index_arn"];

  string index_name = 7 [json_name = "index_name"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 8;

  map<string, string> tags = 9;

  map<string, string> tags_all = 10 [json_name = "tags_all"];

  string vector_bucket_name = 11 [json_name = "vector_bucket_name"];

  repeated MetadataConfiguration metadata_configuration = 12 [json_name = "metadata_configuration"];

  google.protobuf.Value for_each = 13 [json_name = "for_each"];

  repeated string depends_on = 14 [json_name = "depends_on"];

  int32 count = 15;

  string provider = 16;

  terraform.v1.Lifecycle lifecycle = 17;

  message EncryptionConfiguration {
    // kms_key_arn: string
    string kms_key_arn = 1 [json_name = "kms_key_arn"];

    // sse_type: string
    string sse_type = 2 [json_name = "sse_type"];
  }

  message MetadataConfiguration {
    repeated string non_filterable_metadata_keys = 1 [json_name = "non_filterable_metadata_keys"];
  }
}

// AwsS3vectorsVectorBucket version is 0
message AwsS3vectorsVectorBucket {
  string creation_time = 1 [json_name = "creation_time"];

  repeated EncryptionConfiguration encryption_configuration = 2 [json_name = "encryption_configuration"];

  bool force_destroy = 3 [json_name = "force_destroy"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 4;

  map<string, string> tags = 5;

  map<string, string> tags_all = 6 [json_name = "tags_all"];

  string vector_bucket_arn = 7 [json_name = "vector_bucket_arn"];

  string vector_bucket_name = 8 [json_name = "vector_bucket_name"];

  google.protobuf.Value for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  int32 count = 11;

  string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message EncryptionConfiguration {
    // kms_key_arn: string
    string kms_key_arn = 1 [json_name = "kms_key_arn"];

    // sse_type: string
    string sse_type = 2 [json_name = "sse_type"];
  }
}

// AwsS3vectorsVectorBucketPolicy version is 0
message AwsS3vectorsVectorBucketPolicy {
  string policy = 1;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 2;

  string vector_bucket_arn = 3 [json_name = "vector_bucket_arn"];

  google.protobuf.Value for_each = 4 [json_name = "for_each"];

  repeated string depends_on = 5 [json_name = "depends_on"];

  int32 count = 6;

  string provider = 7;

  terraform.v1.Lifecycle lifecycle = 8;
}
