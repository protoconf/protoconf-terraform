syntax = "proto3";

// Provider: aws 6
package terraform.aws.resources.v6;

import "google/protobuf/struct.proto";

import "terraform/v1/meta.proto";

// AwsAppfabricAppAuthorization version is 0
message AwsAppfabricAppAuthorization {
  string app = 1;

  string app_bundle_arn = 2 [json_name = "app_bundle_arn"];

  string arn = 3;

  string auth_type = 4 [json_name = "auth_type"];

  string auth_url = 5 [json_name = "auth_url"];

  string created_at = 6 [json_name = "created_at"];

  string id = 7;

  string persona = 8;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 9;

  map<string, string> tags = 10;

  map<string, string> tags_all = 11 [json_name = "tags_all"];

  string updated_at = 12 [json_name = "updated_at"];

  repeated Credential credential = 13;

  repeated Tenant tenant = 14;

  repeated Timeouts timeouts = 15;

  google.protobuf.Value for_each = 16 [json_name = "for_each"];

  repeated string depends_on = 17 [json_name = "depends_on"];

  int32 count = 18;

  string provider = 19;

  terraform.v1.Lifecycle lifecycle = 20;

  message Credential {
    repeated ApiKeyCredential api_key_credential = 1 [json_name = "api_key_credential"];

    repeated Oauth2Credential oauth2_credential = 2 [json_name = "oauth2_credential"];

    message ApiKeyCredential {
      string api_key = 1 [json_name = "api_key"];
    }

    message Oauth2Credential {
      string client_id = 1 [json_name = "client_id"];

      string client_secret = 2 [json_name = "client_secret"];
    }
  }

  message Tenant {
    string tenant_display_name = 1 [json_name = "tenant_display_name"];

    string tenant_identifier = 2 [json_name = "tenant_identifier"];
  }

  message Timeouts {
    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours).
    string create = 1;

    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours). Setting a timeout for a Delete operation is only
    // applicable if changes are saved into state before the destroy operation occurs.
    string delete = 2;

    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours).
    string update = 3;
  }
}

// AwsAppfabricAppAuthorizationConnection version is 0
message AwsAppfabricAppAuthorizationConnection {
  string app = 1;

  string app_authorization_arn = 2 [json_name = "app_authorization_arn"];

  string app_bundle_arn = 3 [json_name = "app_bundle_arn"];

  string id = 4;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 5;

  repeated Tenant tenant = 6;

  repeated AuthRequest auth_request = 7 [json_name = "auth_request"];

  repeated Timeouts timeouts = 8;

  google.protobuf.Value for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  int32 count = 11;

  string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Tenant {
    // tenant_display_name: string
    string tenant_display_name = 1 [json_name = "tenant_display_name"];

    // tenant_identifier: string
    string tenant_identifier = 2 [json_name = "tenant_identifier"];
  }

  message AuthRequest {
    string code = 1;

    string redirect_uri = 2 [json_name = "redirect_uri"];
  }

  message Timeouts {
    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours).
    string create = 1;
  }
}

// AwsAppfabricAppBundle version is 0
message AwsAppfabricAppBundle {
  string arn = 1;

  string customer_managed_key_arn = 2 [json_name = "customer_managed_key_arn"];

  string id = 3;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 4;

  map<string, string> tags = 5;

  map<string, string> tags_all = 6 [json_name = "tags_all"];

  google.protobuf.Value for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  int32 count = 9;

  string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;
}

// AwsAppfabricIngestion version is 0
message AwsAppfabricIngestion {
  string app = 1;

  string app_bundle_arn = 2 [json_name = "app_bundle_arn"];

  string arn = 3;

  string id = 4;

  string ingestion_type = 5 [json_name = "ingestion_type"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 6;

  map<string, string> tags = 7;

  map<string, string> tags_all = 8 [json_name = "tags_all"];

  string tenant_id = 9 [json_name = "tenant_id"];

  google.protobuf.Value for_each = 10 [json_name = "for_each"];

  repeated string depends_on = 11 [json_name = "depends_on"];

  int32 count = 12;

  string provider = 13;

  terraform.v1.Lifecycle lifecycle = 14;
}

// AwsAppfabricIngestionDestination version is 0
message AwsAppfabricIngestionDestination {
  string app_bundle_arn = 1 [json_name = "app_bundle_arn"];

  string arn = 2;

  string id = 3;

  string ingestion_arn = 4 [json_name = "ingestion_arn"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 5;

  map<string, string> tags = 6;

  map<string, string> tags_all = 7 [json_name = "tags_all"];

  repeated DestinationConfiguration destination_configuration = 8 [json_name = "destination_configuration"];

  repeated ProcessingConfiguration processing_configuration = 9 [json_name = "processing_configuration"];

  repeated Timeouts timeouts = 10;

  google.protobuf.Value for_each = 11 [json_name = "for_each"];

  repeated string depends_on = 12 [json_name = "depends_on"];

  int32 count = 13;

  string provider = 14;

  terraform.v1.Lifecycle lifecycle = 15;

  message DestinationConfiguration {
    repeated AuditLog audit_log = 1 [json_name = "audit_log"];

    message AuditLog {
      repeated Destination destination = 1;

      message Destination {
        repeated FirehoseStream firehose_stream = 1 [json_name = "firehose_stream"];

        repeated S3Bucket s3_bucket = 2 [json_name = "s3_bucket"];

        message FirehoseStream {
          string stream_name = 1 [json_name = "stream_name"];
        }

        message S3Bucket {
          string bucket_name = 1 [json_name = "bucket_name"];

          string prefix = 2;
        }
      }
    }
  }

  message ProcessingConfiguration {
    repeated AuditLog audit_log = 1 [json_name = "audit_log"];

    message AuditLog {
      string format = 1;

      string schema = 2;
    }
  }

  message Timeouts {
    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours).
    string create = 1;

    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours). Setting a timeout for a Delete operation is only
    // applicable if changes are saved into state before the destroy operation occurs.
    string delete = 2;

    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours).
    string update = 3;
  }
}
