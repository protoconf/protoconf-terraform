syntax = "proto3";

// Provider: aws 6
package terraform.aws.resources.v6;

import "google/protobuf/struct.proto";

import "terraform/v1/meta.proto";

// AwsComputeoptimizerEnrollmentStatus version is 0
message AwsComputeoptimizerEnrollmentStatus {
  string id = 1;

  bool include_member_accounts = 2 [json_name = "include_member_accounts"];

  int64 number_of_member_accounts_opted_in = 3 [json_name = "number_of_member_accounts_opted_in"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 4;

  string status = 5;

  repeated Timeouts timeouts = 6;

  google.protobuf.Value for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  int32 count = 9;

  string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;

  message Timeouts {
    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours).
    string create = 1;

    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours).
    string update = 2;
  }
}

// AwsComputeoptimizerRecommendationPreferences version is 0
message AwsComputeoptimizerRecommendationPreferences {
  string enhanced_infrastructure_metrics = 1 [json_name = "enhanced_infrastructure_metrics"];

  string id = 2;

  string inferred_workload_types = 3 [json_name = "inferred_workload_types"];

  string look_back_period = 4 [json_name = "look_back_period"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 5;

  string resource_type = 6 [json_name = "resource_type"];

  string savings_estimation_mode = 7 [json_name = "savings_estimation_mode"];

  repeated ExternalMetricsPreference external_metrics_preference = 8 [json_name = "external_metrics_preference"];

  repeated PreferredResource preferred_resource = 9 [json_name = "preferred_resource"];

  repeated Scope scope = 10;

  repeated UtilizationPreference utilization_preference = 11 [json_name = "utilization_preference"];

  google.protobuf.Value for_each = 12 [json_name = "for_each"];

  repeated string depends_on = 13 [json_name = "depends_on"];

  int32 count = 14;

  string provider = 15;

  terraform.v1.Lifecycle lifecycle = 16;

  message ExternalMetricsPreference {
    string source = 1;
  }

  message PreferredResource {
    repeated string exclude_list = 1 [json_name = "exclude_list"];

    repeated string include_list = 2 [json_name = "include_list"];

    string name = 3;
  }

  message Scope {
    string name = 1;

    string value = 2;
  }

  message UtilizationPreference {
    string metric_name = 1 [json_name = "metric_name"];

    repeated MetricParameters metric_parameters = 2 [json_name = "metric_parameters"];

    message MetricParameters {
      string headroom = 1;

      string threshold = 2;
    }
  }
}
