syntax = "proto3";

// Provider: aws 6
package terraform.aws.resources.v6;

import "google/protobuf/struct.proto";

import "terraform/v1/meta.proto";

// AwsPinpointsmsvoicev2ConfigurationSet version is 0
message AwsPinpointsmsvoicev2ConfigurationSet {
  string arn = 1;

  string default_message_type = 2 [json_name = "default_message_type"];

  string default_sender_id = 3 [json_name = "default_sender_id"];

  string id = 4;

  string name = 5;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 6;

  map<string, string> tags = 7;

  map<string, string> tags_all = 8 [json_name = "tags_all"];

  google.protobuf.Value for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  int32 count = 11;

  string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;
}

// AwsPinpointsmsvoicev2OptOutList version is 0
message AwsPinpointsmsvoicev2OptOutList {
  string arn = 1;

  string id = 2;

  string name = 3;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 4;

  map<string, string> tags = 5;

  map<string, string> tags_all = 6 [json_name = "tags_all"];

  google.protobuf.Value for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  int32 count = 9;

  string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;
}

// AwsPinpointsmsvoicev2PhoneNumber version is 0
message AwsPinpointsmsvoicev2PhoneNumber {
  string arn = 1;

  bool deletion_protection_enabled = 2 [json_name = "deletion_protection_enabled"];

  string id = 3;

  string iso_country_code = 4 [json_name = "iso_country_code"];

  string message_type = 5 [json_name = "message_type"];

  string monthly_leasing_price = 6 [json_name = "monthly_leasing_price"];

  repeated string number_capabilities = 7 [json_name = "number_capabilities"];

  string number_type = 8 [json_name = "number_type"];

  string opt_out_list_name = 9 [json_name = "opt_out_list_name"];

  string phone_number = 10 [json_name = "phone_number"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 11;

  string registration_id = 12 [json_name = "registration_id"];

  bool self_managed_opt_outs_enabled = 13 [json_name = "self_managed_opt_outs_enabled"];

  map<string, string> tags = 14;

  map<string, string> tags_all = 15 [json_name = "tags_all"];

  string two_way_channel_arn = 16 [json_name = "two_way_channel_arn"];

  bool two_way_channel_enabled = 17 [json_name = "two_way_channel_enabled"];

  string two_way_channel_role = 18 [json_name = "two_way_channel_role"];

  repeated Timeouts timeouts = 19;

  google.protobuf.Value for_each = 20 [json_name = "for_each"];

  repeated string depends_on = 21 [json_name = "depends_on"];

  int32 count = 22;

  string provider = 23;

  terraform.v1.Lifecycle lifecycle = 24;

  message Timeouts {
    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours).
    string create = 1;

    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours). Setting a timeout for a Delete operation is only
    // applicable if changes are saved into state before the destroy operation occurs.
    string delete = 2;

    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours).
    string update = 3;
  }
}
