syntax = "proto3";

// Provider: aws 6
package terraform.aws.resources.v6;

import "google/protobuf/struct.proto";

import "terraform/v1/meta.proto";

// AwsSecretsmanagerSecret version is 0
message AwsSecretsmanagerSecret {
  string arn = 1;

  string description = 2;

  bool force_overwrite_replica_secret = 3 [json_name = "force_overwrite_replica_secret"];

  string id = 4;

  string kms_key_id = 5 [json_name = "kms_key_id"];

  string name = 6;

  string name_prefix = 7 [json_name = "name_prefix"];

  string policy = 8;

  int64 recovery_window_in_days = 9 [json_name = "recovery_window_in_days"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 10;

  map<string, string> tags = 11;

  map<string, string> tags_all = 12 [json_name = "tags_all"];

  repeated Replica replica = 13;

  google.protobuf.Value for_each = 14 [json_name = "for_each"];

  repeated string depends_on = 15 [json_name = "depends_on"];

  int32 count = 16;

  string provider = 17;

  terraform.v1.Lifecycle lifecycle = 18;

  message Replica {
    string kms_key_id = 1 [json_name = "kms_key_id"];

    string last_accessed_date = 2 [json_name = "last_accessed_date"];

    string region = 3;

    string status = 4;

    string status_message = 5 [json_name = "status_message"];
  }
}

// AwsSecretsmanagerSecretPolicy version is 0
message AwsSecretsmanagerSecretPolicy {
  bool block_public_policy = 1 [json_name = "block_public_policy"];

  string id = 2;

  string policy = 3;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 4;

  string secret_arn = 5 [json_name = "secret_arn"];

  google.protobuf.Value for_each = 6 [json_name = "for_each"];

  repeated string depends_on = 7 [json_name = "depends_on"];

  int32 count = 8;

  string provider = 9;

  terraform.v1.Lifecycle lifecycle = 10;
}

// AwsSecretsmanagerSecretRotation version is 1
message AwsSecretsmanagerSecretRotation {
  string id = 1;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 2;

  bool rotate_immediately = 3 [json_name = "rotate_immediately"];

  bool rotation_enabled = 4 [json_name = "rotation_enabled"];

  string rotation_lambda_arn = 5 [json_name = "rotation_lambda_arn"];

  string secret_id = 6 [json_name = "secret_id"];

  RotationRules rotation_rules = 7 [json_name = "rotation_rules"];

  google.protobuf.Value for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  int32 count = 10;

  string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;

  message RotationRules {
    int64 automatically_after_days = 1 [json_name = "automatically_after_days"];

    string duration = 2;

    string schedule_expression = 3 [json_name = "schedule_expression"];
  }
}

// AwsSecretsmanagerSecretVersion version is 0
message AwsSecretsmanagerSecretVersion {
  string arn = 1;

  bool has_secret_string_wo = 2 [json_name = "has_secret_string_wo"];

  string id = 3;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 4;

  string secret_binary = 5 [json_name = "secret_binary"];

  string secret_id = 6 [json_name = "secret_id"];

  string secret_string = 7 [json_name = "secret_string"];

  string secret_string_wo = 8 [json_name = "secret_string_wo"];

  int64 secret_string_wo_version = 9 [json_name = "secret_string_wo_version"];

  string version_id = 10 [json_name = "version_id"];

  repeated string version_stages = 11 [json_name = "version_stages"];

  google.protobuf.Value for_each = 12 [json_name = "for_each"];

  repeated string depends_on = 13 [json_name = "depends_on"];

  int32 count = 14;

  string provider = 15;

  terraform.v1.Lifecycle lifecycle = 16;
}

// AwsSecretsmanagerTag version is 0
message AwsSecretsmanagerTag {
  string id = 1;

  string key = 2;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 3;

  string secret_id = 4 [json_name = "secret_id"];

  string value = 5;

  google.protobuf.Value for_each = 6 [json_name = "for_each"];

  repeated string depends_on = 7 [json_name = "depends_on"];

  int32 count = 8;

  string provider = 9;

  terraform.v1.Lifecycle lifecycle = 10;
}
