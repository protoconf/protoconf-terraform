syntax = "proto3";

// Provider: aws 6
package terraform.aws.resources.v6;

import "google/protobuf/struct.proto";

import "terraform/v1/meta.proto";

// AwsSesActiveReceiptRuleSet version is 0
message AwsSesActiveReceiptRuleSet {
  string arn = 1;

  string id = 2;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 3;

  string rule_set_name = 4 [json_name = "rule_set_name"];

  google.protobuf.Value for_each = 5 [json_name = "for_each"];

  repeated string depends_on = 6 [json_name = "depends_on"];

  int32 count = 7;

  string provider = 8;

  terraform.v1.Lifecycle lifecycle = 9;
}

// AwsSesConfigurationSet version is 0
message AwsSesConfigurationSet {
  string arn = 1;

  string id = 2;

  string last_fresh_start = 3 [json_name = "last_fresh_start"];

  string name = 4;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 5;

  bool reputation_metrics_enabled = 6 [json_name = "reputation_metrics_enabled"];

  bool sending_enabled = 7 [json_name = "sending_enabled"];

  DeliveryOptions delivery_options = 8 [json_name = "delivery_options"];

  TrackingOptions tracking_options = 9 [json_name = "tracking_options"];

  google.protobuf.Value for_each = 10 [json_name = "for_each"];

  repeated string depends_on = 11 [json_name = "depends_on"];

  int32 count = 12;

  string provider = 13;

  terraform.v1.Lifecycle lifecycle = 14;

  message DeliveryOptions {
    string tls_policy = 1 [json_name = "tls_policy"];
  }

  message TrackingOptions {
    string custom_redirect_domain = 1 [json_name = "custom_redirect_domain"];
  }
}

// AwsSesDomainDkim version is 0
message AwsSesDomainDkim {
  repeated string dkim_tokens = 1 [json_name = "dkim_tokens"];

  string domain = 2;

  string id = 3;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 4;

  google.protobuf.Value for_each = 5 [json_name = "for_each"];

  repeated string depends_on = 6 [json_name = "depends_on"];

  int32 count = 7;

  string provider = 8;

  terraform.v1.Lifecycle lifecycle = 9;
}

// AwsSesDomainIdentity version is 0
message AwsSesDomainIdentity {
  string arn = 1;

  string domain = 2;

  string id = 3;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 4;

  string verification_token = 5 [json_name = "verification_token"];

  google.protobuf.Value for_each = 6 [json_name = "for_each"];

  repeated string depends_on = 7 [json_name = "depends_on"];

  int32 count = 8;

  string provider = 9;

  terraform.v1.Lifecycle lifecycle = 10;
}

// AwsSesDomainIdentityVerification version is 0
message AwsSesDomainIdentityVerification {
  string arn = 1;

  string domain = 2;

  string id = 3;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 4;

  repeated Timeouts timeouts = 5;

  google.protobuf.Value for_each = 6 [json_name = "for_each"];

  repeated string depends_on = 7 [json_name = "depends_on"];

  int32 count = 8;

  string provider = 9;

  terraform.v1.Lifecycle lifecycle = 10;

  message Timeouts {
    string create = 1;
  }
}

// AwsSesDomainMailFrom version is 0
message AwsSesDomainMailFrom {
  string behavior_on_mx_failure = 1 [json_name = "behavior_on_mx_failure"];

  string domain = 2;

  string id = 3;

  string mail_from_domain = 4 [json_name = "mail_from_domain"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 5;

  google.protobuf.Value for_each = 6 [json_name = "for_each"];

  repeated string depends_on = 7 [json_name = "depends_on"];

  int32 count = 8;

  string provider = 9;

  terraform.v1.Lifecycle lifecycle = 10;
}

// AwsSesEmailIdentity version is 0
message AwsSesEmailIdentity {
  string arn = 1;

  string email = 2;

  string id = 3;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 4;

  google.protobuf.Value for_each = 5 [json_name = "for_each"];

  repeated string depends_on = 6 [json_name = "depends_on"];

  int32 count = 7;

  string provider = 8;

  terraform.v1.Lifecycle lifecycle = 9;
}

// AwsSesEventDestination version is 0
message AwsSesEventDestination {
  string arn = 1;

  string configuration_set_name = 2 [json_name = "configuration_set_name"];

  bool enabled = 3;

  string id = 4;

  repeated string matching_types = 5 [json_name = "matching_types"];

  string name = 6;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 7;

  repeated CloudwatchDestination cloudwatch_destination = 8 [json_name = "cloudwatch_destination"];

  KinesisDestination kinesis_destination = 9 [json_name = "kinesis_destination"];

  SnsDestination sns_destination = 10 [json_name = "sns_destination"];

  google.protobuf.Value for_each = 11 [json_name = "for_each"];

  repeated string depends_on = 12 [json_name = "depends_on"];

  int32 count = 13;

  string provider = 14;

  terraform.v1.Lifecycle lifecycle = 15;

  message CloudwatchDestination {
    string default_value = 1 [json_name = "default_value"];

    string dimension_name = 2 [json_name = "dimension_name"];

    string value_source = 3 [json_name = "value_source"];
  }

  message KinesisDestination {
    string role_arn = 1 [json_name = "role_arn"];

    string stream_arn = 2 [json_name = "stream_arn"];
  }

  message SnsDestination {
    string topic_arn = 1 [json_name = "topic_arn"];
  }
}

// AwsSesIdentityNotificationTopic version is 0
message AwsSesIdentityNotificationTopic {
  string id = 1;

  string identity = 2;

  bool include_original_headers = 3 [json_name = "include_original_headers"];

  string notification_type = 4 [json_name = "notification_type"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 5;

  string topic_arn = 6 [json_name = "topic_arn"];

  google.protobuf.Value for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  int32 count = 9;

  string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;
}

// AwsSesIdentityPolicy version is 0
message AwsSesIdentityPolicy {
  string id = 1;

  string identity = 2;

  string name = 3;

  string policy = 4;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 5;

  google.protobuf.Value for_each = 6 [json_name = "for_each"];

  repeated string depends_on = 7 [json_name = "depends_on"];

  int32 count = 8;

  string provider = 9;

  terraform.v1.Lifecycle lifecycle = 10;
}

// AwsSesReceiptFilter version is 0
message AwsSesReceiptFilter {
  string arn = 1;

  string cidr = 2;

  string id = 3;

  string name = 4;

  string policy = 5;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 6;

  google.protobuf.Value for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  int32 count = 9;

  string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;
}

// AwsSesReceiptRule version is 0
message AwsSesReceiptRule {
  string after = 1;

  string arn = 2;

  bool enabled = 3;

  string id = 4;

  string name = 5;

  repeated string recipients = 6;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 7;

  string rule_set_name = 8 [json_name = "rule_set_name"];

  bool scan_enabled = 9 [json_name = "scan_enabled"];

  string tls_policy = 10 [json_name = "tls_policy"];

  repeated AddHeaderAction add_header_action = 11 [json_name = "add_header_action"];

  repeated BounceAction bounce_action = 12 [json_name = "bounce_action"];

  repeated LambdaAction lambda_action = 13 [json_name = "lambda_action"];

  repeated S3Action s3_action = 14 [json_name = "s3_action"];

  repeated SnsAction sns_action = 15 [json_name = "sns_action"];

  repeated StopAction stop_action = 16 [json_name = "stop_action"];

  repeated WorkmailAction workmail_action = 17 [json_name = "workmail_action"];

  google.protobuf.Value for_each = 18 [json_name = "for_each"];

  repeated string depends_on = 19 [json_name = "depends_on"];

  int32 count = 20;

  string provider = 21;

  terraform.v1.Lifecycle lifecycle = 22;

  message AddHeaderAction {
    string header_name = 1 [json_name = "header_name"];

    string header_value = 2 [json_name = "header_value"];

    int64 position = 3;
  }

  message BounceAction {
    string message = 1;

    int64 position = 2;

    string sender = 3;

    string smtp_reply_code = 4 [json_name = "smtp_reply_code"];

    string status_code = 5 [json_name = "status_code"];

    string topic_arn = 6 [json_name = "topic_arn"];
  }

  message LambdaAction {
    string function_arn = 1 [json_name = "function_arn"];

    string invocation_type = 2 [json_name = "invocation_type"];

    int64 position = 3;

    string topic_arn = 4 [json_name = "topic_arn"];
  }

  message S3Action {
    string bucket_name = 1 [json_name = "bucket_name"];

    string iam_role_arn = 2 [json_name = "iam_role_arn"];

    string kms_key_arn = 3 [json_name = "kms_key_arn"];

    string object_key_prefix = 4 [json_name = "object_key_prefix"];

    int64 position = 5;

    string topic_arn = 6 [json_name = "topic_arn"];
  }

  message SnsAction {
    string encoding = 1;

    int64 position = 2;

    string topic_arn = 3 [json_name = "topic_arn"];
  }

  message StopAction {
    int64 position = 1;

    string scope = 2;

    string topic_arn = 3 [json_name = "topic_arn"];
  }

  message WorkmailAction {
    string organization_arn = 1 [json_name = "organization_arn"];

    int64 position = 2;

    string topic_arn = 3 [json_name = "topic_arn"];
  }
}

// AwsSesReceiptRuleSet version is 0
message AwsSesReceiptRuleSet {
  string arn = 1;

  string id = 2;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 3;

  string rule_set_name = 4 [json_name = "rule_set_name"];

  google.protobuf.Value for_each = 5 [json_name = "for_each"];

  repeated string depends_on = 6 [json_name = "depends_on"];

  int32 count = 7;

  string provider = 8;

  terraform.v1.Lifecycle lifecycle = 9;
}

// AwsSesTemplate version is 0
message AwsSesTemplate {
  string arn = 1;

  string html = 2;

  string id = 3;

  string name = 4;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 5;

  string subject = 6;

  string text = 7;

  google.protobuf.Value for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  int32 count = 10;

  string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;
}
