syntax = "proto3";

// Provider: aws 6
package terraform.aws.resources.v6;

import "google/protobuf/struct.proto";

import "terraform/v1/meta.proto";

// AwsServicequotasAutoManagement version is 0
message AwsServicequotasAutoManagement {
  map<string, string> exclusion_list = 1 [json_name = "exclusion_list"];

  string id = 2;

  string notification_arn = 3 [json_name = "notification_arn"];

  string opt_in_level = 4 [json_name = "opt_in_level"];

  string opt_in_type = 5 [json_name = "opt_in_type"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 6;

  google.protobuf.Value for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  int32 count = 9;

  string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;
}

// AwsServicequotasServiceQuota version is 0
message AwsServicequotasServiceQuota {
  bool adjustable = 1;

  string arn = 2;

  int64 default_value = 3 [json_name = "default_value"];

  string id = 4;

  string quota_code = 5 [json_name = "quota_code"];

  string quota_name = 6 [json_name = "quota_name"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 7;

  string request_id = 8 [json_name = "request_id"];

  string request_status = 9 [json_name = "request_status"];

  string service_code = 10 [json_name = "service_code"];

  string service_name = 11 [json_name = "service_name"];

  repeated UsageMetric usage_metric = 12 [json_name = "usage_metric"];

  int64 value = 13;

  google.protobuf.Value for_each = 14 [json_name = "for_each"];

  repeated string depends_on = 15 [json_name = "depends_on"];

  int32 count = 16;

  string provider = 17;

  terraform.v1.Lifecycle lifecycle = 18;

  message UsageMetric {
    // metric_dimensions: list of object
    repeated MetricDimensions metric_dimensions = 1 [json_name = "metric_dimensions"];

    // metric_name: string
    string metric_name = 2 [json_name = "metric_name"];

    // metric_namespace: string
    string metric_namespace = 3 [json_name = "metric_namespace"];

    // metric_statistic_recommendation: string
    string metric_statistic_recommendation = 4 [json_name = "metric_statistic_recommendation"];

    message MetricDimensions {
      // class: string
      string class = 1;

      // resource: string
      string resource = 2;

      // service: string
      string service = 3;

      // type: string
      string type = 4;
    }
  }
}

// AwsServicequotasTemplate version is 0
message AwsServicequotasTemplate {
  string aws_region = 1 [json_name = "aws_region"];

  bool global_quota = 2 [json_name = "global_quota"];

  string id = 3;

  string quota_code = 4 [json_name = "quota_code"];

  string quota_name = 5 [json_name = "quota_name"];

  string region = 6;

  string service_code = 7 [json_name = "service_code"];

  string service_name = 8 [json_name = "service_name"];

  string unit = 9;

  int64 value = 10;

  google.protobuf.Value for_each = 11 [json_name = "for_each"];

  repeated string depends_on = 12 [json_name = "depends_on"];

  int32 count = 13;

  string provider = 14;

  terraform.v1.Lifecycle lifecycle = 15;
}

// AwsServicequotasTemplateAssociation version is 0
message AwsServicequotasTemplateAssociation {
  string id = 1;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 2;

  bool skip_destroy = 3 [json_name = "skip_destroy"];

  string status = 4;

  google.protobuf.Value for_each = 5 [json_name = "for_each"];

  repeated string depends_on = 6 [json_name = "depends_on"];

  int32 count = 7;

  string provider = 8;

  terraform.v1.Lifecycle lifecycle = 9;
}
