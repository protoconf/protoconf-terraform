syntax = "proto3";

// Provider: aws 6
package terraform.aws.resources.v6;

import "google/protobuf/struct.proto";

import "terraform/v1/meta.proto";

// AwsDrsReplicationConfigurationTemplate version is 0
message AwsDrsReplicationConfigurationTemplate {
  string arn = 1;

  bool associate_default_security_group = 2 [json_name = "associate_default_security_group"];

  bool auto_replicate_new_disks = 3 [json_name = "auto_replicate_new_disks"];

  int64 bandwidth_throttling = 4 [json_name = "bandwidth_throttling"];

  bool create_public_ip = 5 [json_name = "create_public_ip"];

  string data_plane_routing = 6 [json_name = "data_plane_routing"];

  string default_large_staging_disk_type = 7 [json_name = "default_large_staging_disk_type"];

  string ebs_encryption = 8 [json_name = "ebs_encryption"];

  string ebs_encryption_key_arn = 9 [json_name = "ebs_encryption_key_arn"];

  string id = 10;

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 11;

  string replication_server_instance_type = 12 [json_name = "replication_server_instance_type"];

  repeated string replication_servers_security_groups_ids = 13 [
    json_name = "replication_servers_security_groups_ids"
  ];

  string staging_area_subnet_id = 14 [json_name = "staging_area_subnet_id"];

  map<string, string> staging_area_tags = 15 [json_name = "staging_area_tags"];

  map<string, string> tags = 16;

  map<string, string> tags_all = 17 [json_name = "tags_all"];

  bool use_dedicated_replication_server = 18 [json_name = "use_dedicated_replication_server"];

  repeated PitPolicy pit_policy = 19 [json_name = "pit_policy"];

  repeated Timeouts timeouts = 20;

  google.protobuf.Value for_each = 21 [json_name = "for_each"];

  repeated string depends_on = 22 [json_name = "depends_on"];

  int32 count = 23;

  string provider = 24;

  terraform.v1.Lifecycle lifecycle = 25;

  message PitPolicy {
    bool enabled = 1;

    int64 interval = 2;

    int64 retention_duration = 3 [json_name = "retention_duration"];

    int64 rule_id = 4 [json_name = "rule_id"];

    string units = 5;
  }

  message Timeouts {
    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours).
    string create = 1;

    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours). Setting a timeout for a Delete operation is only
    // applicable if changes are saved into state before the destroy operation occurs.
    string delete = 2;

    // A string that can be [parsed as a
    // duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit
    // suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m"
    // (minutes), "h" (hours).
    string update = 3;
  }
}
