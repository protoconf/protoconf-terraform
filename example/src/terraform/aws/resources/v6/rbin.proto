syntax = "proto3";

// Provider: aws 6
package terraform.aws.resources.v6;

import "google/protobuf/struct.proto";

import "terraform/v1/meta.proto";

// AwsRbinRule version is 0
message AwsRbinRule {
  string arn = 1;

  string description = 2;

  string id = 3;

  string lock_end_time = 4 [json_name = "lock_end_time"];

  string lock_state = 5 [json_name = "lock_state"];

  // Region where this resource will be
  // ed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints).
  // Defaults to the Region set in the [provider
  // ry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference).
  string region = 6;

  string resource_type = 7 [json_name = "resource_type"];

  string status = 8;

  map<string, string> tags = 9;

  map<string, string> tags_all = 10 [json_name = "tags_all"];

  repeated ExcludeResourceTags exclude_resource_tags = 11 [json_name = "exclude_resource_tags"];

  LockConfiguration lock_configuration = 12 [json_name = "lock_configuration"];

  repeated ResourceTags resource_tags = 13 [json_name = "resource_tags"];

  RetentionPeriod retention_period = 14 [json_name = "retention_period"];

  repeated Timeouts timeouts = 15;

  google.protobuf.Value for_each = 16 [json_name = "for_each"];

  repeated string depends_on = 17 [json_name = "depends_on"];

  int32 count = 18;

  string provider = 19;

  terraform.v1.Lifecycle lifecycle = 20;

  message ExcludeResourceTags {
    string resource_tag_key = 1 [json_name = "resource_tag_key"];

    string resource_tag_value = 2 [json_name = "resource_tag_value"];
  }

  message LockConfiguration {
    UnlockDelay unlock_delay = 1 [json_name = "unlock_delay"];

    message UnlockDelay {
      string unlock_delay_unit = 1 [json_name = "unlock_delay_unit"];

      int64 unlock_delay_value = 2 [json_name = "unlock_delay_value"];
    }
  }

  message ResourceTags {
    string resource_tag_key = 1 [json_name = "resource_tag_key"];

    string resource_tag_value = 2 [json_name = "resource_tag_value"];
  }

  message RetentionPeriod {
    string retention_period_unit = 1 [json_name = "retention_period_unit"];

    int64 retention_period_value = 2 [json_name = "retention_period_value"];
  }

  message Timeouts {
    string create = 1;

    string delete = 2;

    string update = 3;
  }
}
